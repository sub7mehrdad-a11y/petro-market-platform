# کارنامه‌ی کار (WORKLOG)

این فایل حافظه‌ی بیرونی پروژه است. **قاعده:** بعد از هر مرحله‌ای که کار جلو می‌رود و روی گیت‌هاب
پوش می‌شود، یک ورودی جدید در بالای بخش «ورودی‌ها» اضافه می‌شود: چه کاری شد، چرا، چه فایل‌هایی
عوض شد، و هش کامیت. هدف این است که اگر چت بسته یا پاک شد، در پنجره‌ی جدید بدون از دست رفتن
زمینه ادامه بدهیم.

## نکات ثابت پروژه

- مخزن: https://github.com/sub7mehrdad-a11y/petro-market-platform — شاخه‌ی `main`
- مسیر محلی: `E:\مهرداد\سپهران\پلتفرم تحقیق و توسعه`
- استک: اسکریپت‌های پایتون (`scripts/`) + سایت Next.js (`web/`) + داده‌ی JSON (`data/`) + گزارش‌ها (`reports/`)
- موتور هوش مصنوعی: **Gemini** برای ایجنت‌های روزانه، **Groq** برای «پاسخ هوشمند» `/ask` (عمداً جدا، برای نسوختن سهمیه)
- دیپلوی: **Docker روی Liara** (Vercel به‌خاطر الزام احراز هویت موبایل کنار گذاشته شد)
- دلیل هر تصمیم معماری در `README.md` ثبت شده است.
- **مرورگر داخلی باز نشود** — قبلاً باعث کرش و پاک شدن کل تاریخچه‌ی چت شده است.

## ورودی‌ها

### ۲۰۲۶-۰۹-۰۲ (۶) — سه میانگین به‌جای یک میانگین جهانی برای برآورد مصرف

**زمینه:** کاربر درست گفت میانگین جهانی ثابت (۰.۸۴ کیلوگرم/نفر برای همه‌ی کشورهای بدون رقم
مستقیم) تلورانس خیلی زیادی داره — یک کشور اروپایی و یک کشور کم‌درآمد آفریقایی نباید عدد یکسان
بگیرن. خواست سه میانگین جدا: توسعه‌یافته/اروپایی، در حال توسعه، آفریقا/کم‌درآمد.

**چه شد:**
- `ingest_country_population.py` حالا `incomeLevel` هر کشور رو هم از همون فراخوانی World Bank
  می‌گیره (قبلاً فقط جمعیت) و به سه tier نگاشت می‌کنه: `developed` (High income)،
  `developing` (Upper+Lower middle income)، `least_developed` (Low income) — ۷۱/۱۰۳/۲۴ کشور.
- `per_capita_consumption.json`: بلوک `tier_averages` اضافه شد — `developed: 5.25` (میانگین
  بازه‌ی صریح خودِ گزارش «۴ تا ۶.۵»)، `developing: 1.3` (میانگین بازه‌ی «کمتر از ۱ تا ۲»
  گزارش)، `least_developed: 0.3` (**برآورد داخلی، نه مستقیم از گزارش** — چون گزارش رقمی برای
  این سطح نداشت؛ پایین‌تر از کف بازه‌ی توسعه‌یافته، نزدیک پایین‌ترین ارقام واقعی مشاهده‌شده).
- `getPerCapitaConsumption()` حالا برای کشورهای بدون رقم مستقیم، اول تیر کشور رو نگاه می‌کنه
  و از میانگین همون تیر استفاده می‌کنه؛ فقط اگه تیر نامشخص بود به میانگین جهانی برمی‌گرده.
- برچسب صفحه‌ی کشور هم به‌روز شد تا اسم تیر («میانگین در حال توسعه»، «میانگین کم‌درآمد») رو
  صریح نشون بده، نه فقط «میانگین جهانی».

**تست:** عراق (در حال توسعه) از ۳۹٬۴۹۷ به ۶۱٬۱۲۷ تن تغییر کرد (رقم واقعی‌تر). اتیوپی و چاد
(کم‌درآمد) درست تیر least_developed گرفتن؛ نیجریه با اینکه معمولاً «آفریقایی» تصور می‌شه، طبق
طبقه‌بندی رسمی World Bank «Lower middle income» است، پس در تیر developing قرار گرفت — یک
نکته‌ی واقعی که ارزش دونستنش رو داره (طبقه‌بندی رسمی گاهی با تصور کلی فرق داره).

**فایل‌ها:** `scripts/ingest_country_population.py`، `data/country_population.json`،
`data/per_capita_consumption.json`، `web/lib/data.js`، `web/app/countries/[country]/page.jsx`.

---

### ۲۰۲۶-۰۹-۰۳ (۱) — استخراج و طبقه‌بندی خودکار لیست شرکت‌کنندگان نمایشگاه‌ها

**زمینه:** کاربر خواست از صفحه‌ی «Exhibitor List» نمایشگاه‌های مختلف (شروع با Gulfood 2026)،
شرکت‌های مرتبط با مصارف غذایی جوش شیرین استخراج بشن — هم تولیدکننده هم بازرگان/توزیع‌کننده
(کاربر صراحتاً گفت بازرگان‌ها مشتری بهترن چون توزیع‌کننده‌ی بازار محلی‌شونن)، به‌همراه ایمیل/
وب‌سایت/واتساپ هر شرکت از روی سایت خودش.

**چه شد (خلاصه‌ی مسیر، چون چند بار مسیر عوض شد):**
- `scripts/exhibitor_lead_finder.py` نوشته شد: بدون مرورگر، مستقیم با httpx به endpoint داخلی
  AJAX سایت نمایشگاه (`fetchExhibitors`) وصل می‌شه. اول امتحان شد با فیلتر برچسب‌های کلی
  (Bakery/Beverages/...) که تقریباً هیچی رو کنار نمی‌ذاشت (۵۱۶۱ از ۶۵۰۰ رد شدن) — چون
  Gulfood تقریباً کلاً غذاییه. راه‌حل نهایی: فیلتر فقط روی زیردسته‌های خیلی مشخص (leaf، نه
  سرشاخه) + یک پاس LLM واقعی (نه کلیدواژه) که بخونه و قضاوت کنه تولیدکننده/بازرگان مرتبطه یا نه.
- **Checkpoint اجباری شد**: چون این محیط اجرا (نشست ابزار) چند بار وسط کار قطع شد و پیشرفت از
  بین رفت، بعد از هر بچ نتیجه فوری در `<اسلاگ>.progress.json` ذخیره می‌شه؛ اجرای بعدی از همون‌جا
  ادامه می‌ده، نه از صفر.
- **مشکل سهمیه‌ی Groq**: اولین اجرای کامل با کلید مشترک Groq، با کمتر از ۱۰٪ کار کل سهمیه‌ی
  روزانه‌ی حساب (۲۰۰هزار توکن) رو مصرف کرد — همون حسابی که بخش «پاسخ هوشمند» زنده‌ی سایت
  (`web/app/api/ask`) هم ازش استفاده می‌کنه. کاربر یک کلید Groq اختصاصی جدید داد
  (`GROQ_API_KEY_EXHIBITOR` در `.env`، جدا از `GROQ_API_KEY`/`GROQ_API_KEY_ASK`) ولی اون هم به
  همون سقف ۲۰۰هزار روزانه خورد — معلوم شد این سقف رایگان ظاهراً برای هر حساب Groq یکسانه.
  نتیجه: **Gemini موتور پیش‌فرض شد** (`--engine gemini`، همونی که بقیه‌ی ربات‌های روزانه استفاده
  می‌کنن) چون بدون هیچ خطای نرخ/سهمیه کار کرد؛ Groq با `--engine groq` به‌عنوان جایگزین می‌مونه.
- یک باگ واقعی هم پیدا و رفع شد: یک لینک وب‌سایت شرکت با کاراکتر یونیکد نامعتبر باعث کرش کامل
  اسکریپت با `httpx.InvalidURL` می‌شد (except فقط `httpx.HTTPError` رو می‌گرفت). حالا هم لینک
  sanitize می‌شه هم استثناهای پاس تماس‌گیری با `except Exception` عمومی گرفته می‌شن (سایت‌های
  بیرونی غیرقابل‌اعتمادن، نباید کل اجرا رو کرش بدن).
- **مرحله‌ی استخراج تماس** اضافه شد: از پروفایل نمایشگاه لینک «VISIT WEBSITE» گرفته می‌شه، بعد
  خودِ سایت شرکت (+ چند مسیر رایج `/contact`) برای ایمیل/`tel:`/`wa.me` جست‌وجو می‌شه. پوشش
  ذاتاً جزئیه (بعضی سایت‌ها بلاک/تایم‌اوت/فقط فرم تماس دارن) — صادقانه با `contact_note` ثبت می‌شه.
- **خروجی اکسل جدا برای هر نمایشگاه**: طبق درخواست کاربر، در `E:\مهرداد\سپهران\نمایشگاه دارها\`
  (بیرون از مخزن) ساخته/به‌روز می‌شه. مسیر با متغیر محیطی `EXHIBITOR_XLSX_DIR` قابل override است
  تا روی GitHub Actions به یک مسیر داخل مخزن (`data/exhibition_leads/xlsx/`) بره.
- **اجرای آنلاین**: چون کاربر نمی‌خواست به روشن بودن لپ‌تاپش وابسته باشه،
  `.github/workflows/exhibitor_leads.yml` اضافه شد — هر ۳ ساعت (یا دستی از تب Actions) اجرا
  می‌شه، با `--deadline-minutes 320` تمیز قبل از سقف زمانی CI (۳۴۰ دقیقه) متوقف می‌شه، پیشرفت رو
  commit/push می‌کنه، و فایل اکسل رو هم به‌عنوان Artifact قابل‌دانلود می‌ذاره.

**وضعیت در لحظه‌ی پوش:** از ۲۵۳۲ کاندید (بعد از فیلتر دسته‌بندی)، ۱۲۷۶ تا طبقه‌بندی شده (۱۱۲۹
مرتبط)، همه‌ی ۱۱۲۹ تا هم برای تماس بررسی شدن. بقیه رو GitHub Actions به‌صورت خودکار ادامه می‌ده.

**فایل‌ها:** `scripts/exhibitor_lead_finder.py` (جدید)، `scripts/groq_utils.py` (پارامتر
`api_key` اضافه شد)، `.github/workflows/exhibitor_leads.yml` (جدید)، `.env`/`.env.example`
(کلید `GROQ_API_KEY_EXHIBITOR`)، `.gitignore` (`scripts/_*.log`)،
`data/exhibition_leads/gulfood-2026.json` + `.progress.json`

**کامیت:** `03f943f`

---

### ۲۰۲۶-۰۹-۰۲ (۵) — برآورد بازار مصرف هر کشور بر پایه‌ی جمعیت

**زمینه:** کاربر تأیید کرد برای کشورهایی که رقم مصرف سرانه‌ی مستقیم نداریم، از جمعیت برای
تخمین بازار مصرف استفاده کنیم — دقیقاً همون پیشنهادی که در ورودی قبلی مطرح شده بود.

**چه شد:**
- `scripts/ingest_country_population.py` — جمعیت ۱۹۸ کشور از World Bank Open Data API (رایگان،
  بدون کلید؛ `restcountries.com` امتحان شد ولی نسخه‌ی رایگانش منسوخ شده). دو فراخوانی لازم بود:
  یکی برای متادیتای کشور (تفکیک کشور واقعی از تجمیع منطقه‌ای مثل «Arab World»)، یکی برای خودِ
  جمعیت (`mrv=1` = آخرین سال موجود). خروجی: `data/country_population.json`.
- `getPerCapitaConsumption()` در `web/lib/data.js` بازنویسی شد: برای ۱۱ کشوری که رقم مستقیم
  دارن همون رو نگه می‌داره؛ برای بقیه، اگه جمعیت کشور رو داشته باشیم، با میانگین جهانی (۰.۸۴
  کیلوگرم/نفر) × جمعیت یک «برآورد بازار مصرف» (تن/سال) می‌سازه — با پرچم صریح
  `is_population_based: true` تا همیشه از رقم واقعی قابل‌تفکیک باشه.
- صفحه‌ی هر کشور حالا یک استت جدید «برآورد بازار مصرف سالانه» (یا «...بر پایه‌ی جمعیت» برای
  کشورهای بدون رقم مستقیم) داره — کنار «صادرات ایران به این کشور» که قبلاً اضافه شده بود، تا
  سهم ایران از بازار تخمینی هر کشور قابل‌مقایسه باشه.

**تست:** مقادیر دستی راستی‌آزمایی شد (آلمان: ۸۳٫۵M نفر × ۶.۵ کیلوگرم = ۵۴۲٬۶۹۳ تن — رقم واقعی
گزارش‌شده؛ عراق: ۴۷M نفر × ۰.۸۴ کیلوگرم = ۳۹٬۴۹۷ تن — برآورد بر پایه‌ی جمعیت). صفحات آلمان،
عراق، ازبکستان، قطر مستقیم fetch و بررسی شدن.

**فایل‌ها:** `scripts/ingest_country_population.py`، `data/country_population.json`،
`web/lib/data.js`، `web/app/countries/[country]/page.jsx`، `README.md`.

---

### ۲۰۲۶-۰۹-۰۲ (۴) — صادرات ایران و مصرف سرانه‌ی جهانی روی صفحه‌ی هر کشور

**زمینه:** کاربر دو خواسته داشت: (۱) بالای صفحه‌ی هر کشوری که آماری داریم، میزان صادرات ایران
به همون کشور مشخص نوشته بشه (نه فقط توی صفحه‌ی خودِ ایران)، (۲) یک گزارش مصرف سرانه‌ی جهانی
جوش شیرین (`آمار جهانی/مصرف جهانی جوش شیرین.docx`) داد تا برای هر کشور تخمین مصرف هم اضافه بشه.

**چه شد:**
- `getIranExportToCountry(country)` در `web/lib/data.js` — از همون `iran_exports.json` قبلی،
  فقط یک ردیف مقصد رو پیدا می‌کنه. توی `CountryStatStrip` صفحه‌ی هر کشور (نه فقط ایران) به‌عنوان
  اولین/برجسته‌ترین آمار نمایش داده می‌شه — طبق خواسته‌ی صریح کاربر («بالای صفحه»).
- گزارش «مصرف جهانی جوش شیرین.docx» با `ingest_reports.py` (نوع detailed) وارد `reports/` شد؛
  الان به‌عنوان دوازدهمین گزارش در سایت قابل‌مشاهده‌ست.
- جدول «مصرف سرانه بر اساس منطقه» همون گزارش (۱۱ کشور + میانگین جهانی) دستی به
  `data/per_capita_consumption.json` منتقل شد، با `getPerCapitaConsumption(country)` جدید.
  **عمداً برون‌یابی نشد به بقیه‌ی ~۱۹۰ کشور** — گزارش خودش می‌گه این ارقام «کل مصرف کشور
  (صنعتی+غذایی+دارویی+خوراک دام) ÷ جمعیت» است، نه مصرف خانگی؛ بدون داده‌ی جمعیت/سطح توسعه‌ی
  هر کشور، ساختن عدد برای بقیه‌ی کشورها گمراه‌کننده می‌شد. باید بعداً با کاربر تصمیم گرفته بشه
  که آیا برون‌یابی (مثلاً بر اساس سطح توسعه یا جمعیت×میانگین جهانی) ارزش داره یا نه.

**تست:** `npm run build` بدون خطا؛ صفحات آلمان (هر دو آمار)، عراق (فقط صادرات ایران)، برزیل
(فقط مصرف سرانه)، ایتالیا (هر دو) مستقیم fetch و تأیید شدن.

**فایل‌ها:** `web/lib/data.js`، `web/app/countries/[country]/page.jsx`، `scripts/ingest_reports.py`،
`data/per_capita_consumption.json`، `گزارش/مصرف جهانی جوش شیرین.docx`، `reports/manifest.json`،
`reports/parsed/*.json` (بازتولید کامل)، `README.md`.

---

### ۲۰۲۶-۰۹-۰۲ (۳) — بخش جدید: صادرات واقعی ایران (آمار گمرک، نه ITC)

**زمینه:** کاربر آمار رسمی گمرک جمهوری اسلامی ایران (IRICA) برای صادرات جوش شیرین رو داد —
سه فایل: یکی تفکیک‌شده به کشور برای ده‌ماهه‌ی ۱۴۰۴ (بدون ارزش دلاری)، و دو فایل «صادرات به
تفکیک تعرفه» برای سال کامل ۱۴۰۲ و ۱۴۰۳ (بدون تفکیک کشور، ولی با ارزش ریالی/دلاری). همچنین
یک فایل خیلی بزرگ‌تر (`export-detail.xlsx`، ۵۳٬۰۰۰+ ردیف، همه‌ی محصولات) که معلوم شد شامل
همون داده‌ی ده‌ماهه‌ی ۱۴۰۴ هست ولی **با تفکیک کشور و ارزش دلاری هم‌زمان** — یعنی از فایل اول
که کاربر مستقیم فرستاده بود کامل‌تره، برای همین به‌جای اون استفاده شد.

**اعتبارسنجی:** کاربر گفت صادرات گرید «خوراکی» (یعنی همه‌چیز به‌جز گرید دارویی تزریقی، کد
تعرفه `28363090`) در ۱۴۰۳ برابر ۳۵٬۵۰۰ تن و در ۱۴۰۲ برابر ۲۳٬۷۰۰ تن بوده. عدد استخراج‌شده
از فایل‌های خام: ۳۵٬۴۹۴ و ۲۳٬۶۶۷ تن — تطابق کامل، یعنی کد تعرفه و فایل‌ها درست تشخیص داده شدن.

**چه ساخته شد:**
- فایل‌های خام کپی شدن به `گزارش/گمرک ایران/` (منبع اصلی، طبق قرارداد پروژه).
- `scripts/ingest_iran_exports.py` — سه نوع فایل رو می‌خونه، با یک نگاشت نام‌های رسمی گمرک
  (مثل «فدراسیون روسيه») به نام‌های استاندارد بقیه‌ی سایت (مثل «روسیه»، هم‌راستا با
  `country_name_map.json`)، و `data/iran_exports.json` رو می‌سازه: هم روند سه‌ساله (۱۴۰۲،
  ۱۴۰۳، ۱۴۰۴-ده‌ماهه با برچسب صریح جزئی‌بودن)، هم تفکیک ۲۰ کشور مقصد برای ۱۴۰۴ (با قیمت
  واحد دلاری هر مقصد).
- `getIranExports()` در `web/lib/data.js`.
- کامپوننت جدید `web/app/components/IranExportSection.jsx` — **جدا از SupplierBreakdown**
  عمداً، چون این یکی دیدگاه صادرکننده‌ست (صادرات خودِ ایران به مقاصد)، نه دیدگاه واردکننده
  (واردات یک کشور از مبادی مختلف) که SupplierBreakdown نشون می‌ده.
- توی `web/app/countries/[country]/page.jsx` فقط برای `country === "ایران"` نمایش داده می‌شه،
  بالای بخش SupplierBreakdown.

**تست:** `npm run build` بدون خطا؛ صفحه‌ی `/countries/ایران` مستقیم fetch و بررسی شد — بخش
جدید با اعداد واقعی (۲۶٬۹۴۵ تن روسیه و...) درست رندر شد.

**فایل‌ها:** `scripts/ingest_iran_exports.py`، `web/lib/data.js`،
`web/app/components/IranExportSection.jsx`، `web/app/countries/[country]/page.jsx`،
`data/iran_exports.json`، `گزارش/گمرک ایران/*.xlsx` (۴ فایل خام)، `README.md`.

---

### ۲۰۲۶-۰۹-۰۲ (۲) — رفع باگ واقعی: ترتیب خط‌های پاراگراف در PDF به‌هم می‌ریخت

**زمینه:** کاربر با اسکرین‌شات واقعی از گوشی نشون داد متن خبرهای گزارش هفتگی «جابجا» هستن —
جمله‌ی اول یک پاراگراف در پایین‌ترین خط چاپ می‌شد، نه بالا. همچنین یک جمله‌ی توسعه‌دهنده‌محور
(«ارز محلی طبق قانون پروژه قاطی نمی‌شود») توی گزارشی که قراره مدیرها بخونن پیدا شد.

**ریشه‌ی باگ (واقعی، نه فرضی):** `fa()` در `weekly_report_pdf.py` کل رشته رو یک‌جا با
`get_display()` به «ترتیب دیداری» (visual order) تبدیل می‌کرد. این ترتیب فقط برای یک خط
درسته. وقتی reportlab `Paragraph` مجبور می‌شد اون رشته‌ی از‌قبل بازچیده‌شده رو با منطق ساده‌ی
LTR به چند خط بشکنه (متن‌های کوتاه تست‌شده قبلی هیچ‌وقت نیاز به شکستن نداشتن، برای همین باگ
دیده نشده بود)، ترتیب *خط‌ها* به‌هم می‌ریخت.

**فیکس:** تابع جدید `fa_wrapped()` — متن *منطقی* (قبل از بیدی) رو خودمون بر اساس عرض واقعی
رندر (`pdfmetrics.stringWidth`) می‌شکنیم، بعد `fa()` رو روی هر خط جداگانه اعمال می‌کنیم و با
`<br/>` به هم وصل می‌کنیم — این‌طوری ترتیب خط‌ها دست‌نخورده می‌مونه. روی تیتر/متن خبر، هر
سلول جدولی (`_price_table`, `_key_prices_table`)، و خط منبع اعمال شد.

**باگ دوم که هم‌زمان کشف شد:** اعداد با جداکننده‌ی هزارگان («۱,۰۴۰») هم همون مشکل بیدی رو
داشتن (به «۰۴۰,۱» برعکس می‌شدن) چون regex ایزوله‌کننده‌ی عدد (`_MULTI_PART_NUMBER_RE`، از
رفع باگ تاریخ در ۰۸-۳۱) ویرگول رو جزو جداکننده‌های مجاز نداشت. اضافه شد.

**جمله‌ی توسعه‌دهنده‌محور:** عنوان بخش «قیمت‌های داخلی» از «(ارز محلی — جدا از دلاری، طبق
قانون پروژه قاطی نمی‌شود)» به «(ارز محلی)» ساده شد — دلیل فنی توی کد می‌مونه، نه توی متنی که
مدیرها می‌بینن.

**تست:** با همون داده‌ی واقعی، PDF دوباره ساخته و همه‌ی ۹ خبر (۵ عمومی + ۴ ترانزیت) خونده شد —
همه‌شون حالا به ترتیب منطقی درست (جمله‌ی اول بالا، آخر پایین) و بدون برعکس‌شدن اعداد چاپ شدن.

**فایل‌ها:** `scripts/weekly_report_pdf.py`.

---

### ۲۰۲۶-۰۹-۰۲ — رفع تکرار کلیشه‌ای «قیمت ۲۵۰ دلار» در متن اخبار تحلیلی

**زمینه:** کاربر بعد از دیدن گزارش هفتگی گفت متن خبرها «اشتباه» و کلیشه‌ای‌ان: تقریباً هر آیتم
با همون جمله‌ی «با توجه به قیمت پایه‌ی ۲۵۰ دلاری FOB جوش شیرین پارس...» تموم می‌شد. خواسته‌اش:
مدل این زمینه رو مثل یک تحلیلگر واقعی توی ذهنش نگه داره، نه این‌که عیناً هر بار بنویسدش؛ و توی
خبرهایی که مستقیم به خودمون مربوطه، نیازی به تکرار قیمت خودمون نیست.

**ریشه‌ی باگ:** `SYSTEM_PROMPT` در `scripts/news_analysis_bot.py` صریحاً می‌گفت «حتماً یک جمله
درباره‌ی تاثیرش... نسبت به قیمت پایه‌ی ۲۵۰ دلار» — یعنی این جمله واقعاً اجباری بود، برای هر
آیتم، بدون توجه به این‌که خبر واقعاً ربطی به قیمت ما داشت یا نه.

**چه شد:** اون خط اجباری حذف شد؛ به‌جاش صراحتاً گفته شده زمینه‌ی شرکت باید فقط پس‌زمینه‌ی
قضاوت تحلیلی باشه، جمله‌ی تاثیر فقط وقتی خبر واقعاً ربط مستقیم داره بیاد، هرگز عبارت کلیشه‌ای
ثابت در چند آیتم پشت‌سرهم تکرار نشه، و در خبرهای مستقیماً مربوط به خودمون قیمتمون تکرار نشه.

**تست:** ۴ آیتم امروز (`2026-09-02`) که با پرامپت قدیمی تولید شده بودن حذف و با پرامپت جدید
دوباره تولید شدن (اجرای واقعی، نه شبیه‌سازی) — هیچ‌کدوم دیگه عبارت «۲۵۰ دلار» رو تکرار نکردن؛
یکی از ۴ تا (خبر ژئوپلیتیک بدون ربط مستقیم) اصلاً جمله‌ی تاثیر نداشت، دقیقاً طبق خواسته.
(نکته: ۴ آیتم قدیمی‌تر تاریخ ۰۸-۳۱ هم در همین فرآیند حذف شدن تا جای امروز باز بشه — تاریخ
واقعی سیستم به ۰۹-۰۲ رسیده بود.)

**فایل‌ها:** `scripts/news_analysis_bot.py`، `data/news_analysis_log.json`.

---

### ۲۰۲۶-۰۸-۳۱ (۳) — بازطراحی ساختار گزارش هفتگی طبق بازخورد کاربر

**زمینه:** کاربر بعد از دریافت نسخه‌ی اول در تلگرام گفت ساختار «خیلی اشتباهه»: نمی‌خواد قیمت
خودمون (۲۵۰ دلار) تکرار بشه، به‌جاش قیمت کشورهای کلیدی (چین، ترکیه، برزیل، روسیه — هر کدوم
هر وقت داده داشت) باید اول بیاد، بعد تغییرات قیمت، بعد مهم‌ترین اخبار و تحلیل، و در آخر جدا
مهم‌ترین اخبار ترانزیت/حمل‌ونقل.

**چه شد:**
- `weekly_report_data.py`: تابع `key_country_prices()` اضافه شد (چهار کشور کلیدی؛ روسیه چون
  هنوز منبع قیمت نداره با «به‌زودی» نشون داده می‌شه، نه حذف می‌شه). تابع `split_news()` اخبار
  رو به دو دسته‌ی عمومی/ترانزیت جدا می‌کنه (بر اساس فیلد `topic`). `biggest_mover` نگه داشته
  شد (برای کپشن تلگرام)، `lowest_fob`/`series_count`/`base_fob_usd` (به‌عنوان آیتم نمایشی) و
  `latest_watch`/رصد ترکیه‌وچین حذف شدن — طبق ساختار جدیدی که کاربر خواست.
- `weekly_report_pdf.py`: `_kpi_cards()` و `_watch_block()` حذف شدن؛ `_key_prices_table()`
  جدید اضافه شد. ترتیب `story` بازچینی شد: قیمت کشورهای کلیدی → تغییرات قیمت → نمودار →
  قیمت‌های داخلی → PageBreak → اخبار عمومی → اخبار ترانزیت.
- `weekly_report_bot.py`: `build_caption()` و لاگ‌های `main()` با شکل جدید داده هماهنگ شدن.

**تست:** `--dry-run` دوباره اجرا و PDF چهارصفحه‌ای بررسی شد؛ بعد یک ارسال واقعی دیگه به
تلگرام کاربر انجام شد تا نسخه‌ی جدید رو مستقیم روی گوشی خودش ببینه.

**باز مونده:** کاربر گفت متن «جابجا و عجیب» بود — بخشی از این احتمالاً همون مشکل ساختاری بالا
بود، ولی احتمال یک باگ رندر دیگر (که فقط توی PDF viewer گوشی دیده می‌شه، نه توی ابزار خواندن
PDF این‌جا) هنوز رد نشده؛ منتظر تأیید/اسکرین‌شات کاربر بعد از دیدن نسخه‌ی جدید.

**فایل‌ها:** `scripts/weekly_report_data.py`، `scripts/weekly_report_pdf.py`،
`scripts/weekly_report_bot.py`.

---

### ۲۰۲۶-۰۸-۳۱ (۲) — ساخت ربات گزارش هفتگی مدیران (تلگرام)

**زمینه:** در یک چت جدا (روی claude.ai، اشتباهی)، کاربر خواسته بود یک ربات گزارش هفتگی
تلگرامی بسازیم؛ آن چت کد را روی ریپوی اشتباه (`takht-e-jamshid`) تست کرده بود و لینک‌های
PDF/patch‌اش هم authenticated و از اینجا غیرقابل‌دسترس بودن. کاربر گفت «خودت از اول بساز» و
توکن واقعی ربات (از @BotFather، bot: `@Sepehranrepbot`) رو داد.

**چه ساخته شد (شش فایل جدید در `scripts/`):**
- `env_utils.py` — لودر سبک `.env` بدون وابستگی (کشف جانبی: هیچ اسکریپت پروژه واقعاً `.env`
  رو نمی‌خوند، فقط `os.environ` — یعنی اجرای محلی طبق README کلید رو پیدا نمی‌کرد).
- `telegram_utils.py` — ارسال پیام/PDF با httpx (نه requests) — عمداً، چون `requests` وقتی
  یک درخواست fail بشه توکن رو داخل پیام خطا می‌ذاره (همون کلاس باگ امنیتی‌ای که در چت دیگه
  در یک پروژه‌ی مشابه پیدا و رفع شده بود)؛ اینجا از اول با httpx + پاک‌سازی صریح توکن از هر
  پیام خطا جلوش گرفته شد.
- `weekly_report_data.py` — آماده‌سازی داده از `data/*.json`، **بدون هیچ فراخوانی AI** (عمدی:
  حتی اگه سهمیه‌ی Gemini/Groq تموم شده باشه، گزارش مدیران سالم می‌ره). مقایسه‌ی هفته‌به‌هفته
  دقیقاً روی رکوردهای بعد از رفع باگ dedup (ورودی قبلی همین روز) تکیه می‌کنه.
- `weekly_report_pdf.py` — PDF فارسی راست‌به‌چپ با reportlab (نه weasyprint، چون weasyprint
  روی ویندوز به نصب دستی GTK3 نیاز داره) + فونت Vazirmatn (دانلود شد از ریپوی رسمی‌اش،
  مجوز OFL، در `scripts/fonts/`) + `arabic-reshaper`/`python-bidi` برای شکل‌دهی متن.
- `weekly_report_bot.py` — ارکستراتور با `--dry-run`.
- `telegram_setup.py` — پیدا کردن شناسه‌ی چت مدیرها (چون ربات نمی‌تونه اول خودش پیام بده).

**دو باگ واقعی که موقع تست PDF واقعی (نه فرضی) پیدا و رفع شد:**
1. **برعکس‌شدن تاریخ در بیدی (bidi):** تاریخ ISO («۲۰۲۶-۰۸-۳۱») وسط جمله‌ی فارسی به‌صورت
   «۳۱-۰۸-۲۰۲۶» رسم می‌شد — چون خط‌تیره جداکننده‌ی خنثاست و بدون ایزوله‌ی صریح، الگوریتم
   دوجهته گروه‌های عددی رو برعکس می‌کنه. رفع شد با ایزوله‌کردن (LRE...PDF) هر بلوک عددی
   چندبخشی قبل از reshape/bidi.
2. **برچسب گمراه‌کننده‌ی نمودار:** وقتی هم «جوش شیرین/چین/FOB» هم «سودا اش/چین/FOB» جزو
   ۴ سری برتر بودن، هر دو با برچسب یکسان «China FOB» نشون داده می‌شدن (غیرقابل‌تفکیک).
   محصول هم به برچسب اضافه شد. هم‌زمان یک باگ backfill هم رفع شد: نقطه‌های قبل از اولین
   داده‌ی هر سری داشتن با صفر پر می‌شدن (یک سقوط جعلی به صفر نشون می‌دادن) — الان با اولین
   مقدار واقعی خودِ همون سری پر می‌شن.

**تست:** `--dry-run` واقعی روی داده‌ی زنده‌ی پلتفرم اجرا شد (نه mock)؛ PDF ۴ صفحه‌ای درست
تولید شد. اتصال واقعی به `api.telegram.org` هم با `getMe` تست شد (۲۰۰ OK، bot معتبر) —
برخلاف چت قبلی که اصلاً نتونسته بود تلگرام رو از محیطش تست کنه.

**چیزی که هنوز نیاز به کاربر داره:** برای تست کامل مسیر ارسال (`send_document`)، حداقل یک
مدیر باید یک‌بار به `@Sepehranrepbot` در تلگرام `/start` بزنه؛ بدون این، `TELEGRAM_CHAT_IDS`
خالیه و ارسال واقعی تست نشده (فقط ساخت PDF و اتصال به API تست شد).

**فایل‌ها:** `scripts/{env_utils,telegram_utils,weekly_report_data,weekly_report_pdf,
weekly_report_bot,telegram_setup}.py`، `scripts/fonts/` (فونت Vazirmatn)، `scripts/
requirements.txt`، `.github/workflows/weekly_report.yml`، `.env.example`، `.gitignore`،
`README.md`، `.env` (توکن ربات — کامیت نشد).

---

### ۲۰۲۶-۰۸-۳۱ — رفع باگ جدی: نوسان قیمت جعلی در `price_history.json`

**زمینه:** در یک چت جدا (روی claude.ai، اشتباهی نه در این پروژه)، کاربر از کلود خواسته بود یک
ربات گزارش هفتگی تلگرامی برای این پلتفرم بسازد. آن چت هنگام کار یک باگ واقعی پیدا کرده بود و
گفته بود «احتمالاً همین اشکال در خود سایت هم هست، بررسی نکردم». من همون فرضیه رو مستقیم روی
این ریپو تست کردم.

**باگ (تأیید شد، واقعی بود):** `scripts/price_intelligence_bot.py` هیچ حذف تکراری‌ای نداشت.
وقتی صفحه‌ی یک منبع (مثلاً Procurement Resource) چند دوره رو با هم نشون می‌داد (هم قیمت ماه
گذشته هم دو ماه پیش)، یا دو منبع متفاوت (Procurement Resource و ChemAnalyst) هر دو برای همون
ترکیب (محصول، کشور، نوع قیمت) عدد می‌دادن، مدل وفادارانه همه رو به‌عنوان رکورد جدا ثبت می‌کرد.
نمونه‌ی واقعی که توی `data/price_history.json` پیدا شد: قیمت CIF آلمان برای جوش شیرین در یک
روز هم‌زمان ۳۰۲.۳۳ (Procurement Resource، ژوئن) و ۳۴۱.۳۳ (ChemAnalyst، سه‌ماهه‌ی دوم) داشت.
هیچ مصرف‌کننده‌ی داده‌ای (نمودار داشبورد در `PriceChart.jsx`، کارت‌های هایلایت در
`PriceSection.jsx`، قیمت‌های زنده‌ی صفحه‌ی گزارش‌ها در `data.js`) بر اساس واقعیت انتخاب
نمی‌کرد — فقط بر اساس ترتیب رکورد توی آرایه، که هر روز فرق می‌کرد. نتیجه: نمودار قیمت آلمان
هر چند روز بین ۳۰۲ و ۳۴۱ دلار (~۱۳٪ اختلاف) بالا و پایین می‌پرید، بدون این‌که قیمت واقعی
تغییری کرده باشه — نوسان بازار کاملاً جعلی.

**فیکس:** دو تابع جدید در `price_intelligence_bot.py`:
- `parse_reported_period()` — رشته‌ی `source_reported_date` (مثل «May 2026»، «Q2 2026»، «Q2
  ending June 2026») رو به عدد قابل‌مقایسه (سال×۱۲+ماه) تبدیل می‌کنه.
- `dedupe_records()` — برای هر (محصول، کشور، نوع قیمت) فقط رکورد با جدیدترین دوره رو نگه
  می‌داره. **نکته‌ی مهم که در تست دوم کشف شد:** اگه فقط با ترتیب آرایه تساوی رو بشکونیم، چون
  مدل هر روز ترتیب خروجی رو عوض می‌کنه، همون منبع هر روز می‌بره یا می‌بازه و دقیقاً همون نوسان
  جعلی برمی‌گرده — پس تساوی حالا با نام منبع (الفبایی، ثابت) شکسته می‌شه، نه با موقعیت آرایه.
  با یک تست مستقیم (دو آرایه‌ی یکسان با ترتیب معکوس) تأیید شد که برنده حالا همیشه یکیه.

**اجرای عطف‌به‌ماسبق:** همین تابع رو روی هر ۱۹ batch موجود در `data/price_history.json` هم
اجرا کردم (نه فقط برای اجراهای آینده) — از ۳۵۴ رکورد به ۲۶۵ رکورد رسید (۸۹ رکورد تکراری/هم‌پوشان
حذف شد). قبل از اجرا از فایل اصلی (که هنوز کامیت نشده بود) با گیت مطمئن شدم قابل‌بازگشته.

**چیزی که عمداً دست نزدم:** دو تاریخ (۲۰۲۶-۰۸-۱۲ و ۲۰۲۶-۰۸-۱۳) هر کدوم **دو batch جدا** دارن
(ربات همون روز دو بار اجرا شده، با فاصله‌ی ۳۰ دقیقه تا ۲ ساعت). این یک مشکل داده‌ی دیگه‌ست
(نه چیزی که گزارش شده بود)؛ چون معلوم نیست کدوم اجرا معتبرتره، چیزی رو ادغام/حذف نکردم — فقط
ثبت می‌کنم که وجود داره.

**تست:** `npm run build` بعد از تغییر داده بدون خطا کامل شد.

**فایل‌ها:** `scripts/price_intelligence_bot.py`، `data/price_history.json`.

---

### ۲۰۲۶-۰۸-۳۰ — افزودن منبع خبری روسیه (RUPEC) به ربات اخبار روزانه

**زمینه:** کاربر برای پژوهش رقیب روسیه، فهرستی از ۱۵+ منبع روسی (پرتال‌های خبری صنعت شیمی،
سایت‌های رسمی تولیدکننده‌ها، سامانه‌های افشای مالی، پرتال‌های لجستیک) که از جمینای گرفته بود
فرستاد و خواست بررسی کنم کدام‌ها برای خزش خودکار روزانه واقعاً قابل‌استفاده‌اند.

**چه شد:** هر منبع مستقیم با `WebFetch` تست شد (نه حدس بر اساس اسم). نتیجه:
- **RUPEC (rupec.ru)** — اخبار پتروشیمی/شیمیایی روسیه با تاریخ، رایگان، بدون پی‌وال. ✅
- **ХимОнлайн (himonline.ru)** و **PortNews (portnews.ru)** — با `WebFetch` جواب دادند، ولی وقتی با
  `fetch_page_text` واقعیِ خود ربات (urllib خام، همان چیزی که واقعاً در تولید اجرا می‌شود) تست شدند،
  هر دو تایم‌اوت دادند (himonline: SSL handshake timeout، portnews: read timeout) — به‌احتمال زیاد
  سیستم ضدربات روسی (شبیه Qrator) که کلاینت‌های ساده را معطل نگه می‌دارد. رد شدند و در کد مستند شد.
- بقیه (Хим-Курьер بخش تخصصی، RZD-Partner، RCC Group، e-disclosure، Checko، Rusprofile) یا پی‌وال
  داشتند، یا ۴۰۳/۵۰۳ دادند، یا اصلاً وصل نشدند — رد شدند.

RUPEC به `SEED_NEWS_SOURCES` در `scripts/news_analysis_bot.py` اضافه شد، و متن `COMPANY_CONTEXT`
به‌روزرسانی شد تا روسیه دیگر «به‌زودی» نباشد بلکه رقیب سوم تحت رصد فعال معرفی شود (پروفایل کامل
تولیدکنندگانش هنوز در `data/competitors.json` نیست — آن بخش منتظر پژوهش کامل روسیه است که کاربر
هفته‌ی بعد می‌فرستد).

**تست:** ربات یک‌بار به‌صورت واقعی (نه شبیه‌سازی) اجرا شد تا مطمئن شویم منبع جدید کرش نمی‌کند —
موفق بود، ۳ خبر تازه‌ی ۲۰۲۶-۰۸-۳۰ ثبت شد (یکی‌شان درباره‌ی بحران سوخت روسیه، از منبع دیگری غیر
از RUPEC).

**فایل‌ها:** `scripts/news_analysis_bot.py`، `data/news_analysis_log.json` (خروجی اجرای تستی).

**قدم بعدی:** وقتی کاربر پژوهش کامل روسیه (تولیدکننده‌ها، ساختار هزینه، صادرات، تحریم) را فرستاد،
صفحه‌ی رقیب روسیه در `data/competitors.json` مثل ترکیه/چین تکمیل می‌شود.

---

### ۲۰۲۶-۰۸-۲۹ (۲) — اصلاح: فایل Trade Map با نسخه‌ی تازه‌تر جایگزین شد

**زمینه:** در مرحله‌ی قبل، فایل دوم («TradeMap Companies Database - Verified & Completed.xlsx»)
را کاربر صریحاً نفرستاده بود — از پوشه پیدا و حدس زده شد (چون هم‌اسکیما و جدیدترین بود). درست
از آب درآمد، ولی کاربر بعداً همین فایل را صریحاً فرستاد تا مطمئن شویم.

**ولی هش فایلی که این‌بار فرستاده شد با نسخه‌ای که در مخزن بود یکی نبود.** بررسی نشان داد
نسخه‌ی تازه ۱۵۲ ردیف دارد (نه ۱۷۱) — ۱۷ شرکت نسبت به نسخه‌ی قبلی حذف شده‌اند (هیچ شرکت تازه‌ای
اضافه نشده)، ظاهراً یک دور پالایش بیشتر روی همان فایل. نمونه‌ی حذف‌شده‌ها: Cargill،
Nippon Solvay K.K.، NAFTA-UKRAINA و چند شرکت دیگر که احتمالاً در بازبینی بیشتر رد صلاحیت شدند.

**چه شد:** فایل قدیمی در `گزارش/لیست شرکتها/` با نسخه‌ی تازه جایگزین و `ingest_companies.py`
دوباره اجرا شد (بدون تغییر کد — پایپ‌لاین از قبل کل خروجی را از منبع بازمی‌سازد).

**نتیجه:** از این منبع ۱۴۸ شرکت در ۲۷ کشور (قبلاً ۱۶۷ شرکت در ۲۹ کشور). جمع کل بانک شرکت‌ها
از ۳۷۱ به **۳۵۲** رسید.

**درس:** وقتی فایلی «حدسی» به پروژه اضافه می‌شود، باید همیشه با هش/محتوا در برابر نسخه‌ای که
کاربر بعداً صریحاً می‌فرستد چک شود — حتی اگر اسم فایل عیناً یکی باشد.

**فایل‌ها:** `گزارش/لیست شرکتها/TradeMap Companies Database - Verified & Completed.xlsx`،
`data/companies.json`

---

### ۲۰۲۶-۰۸-۲۹ — افزودن ۱۷۹ شرکت جهانی از Volza و ITC Trade Map به بانک شرکت‌ها

**ورودی:** دو فایل اکسل که کاربر جداگانه (خارج از این جلسه) با کمک Gemini/Claude تهیه و
راستی‌آزمایی کرده بود: `Verified_Global_Companies.xlsx` (۱۵ شرکت، منبع Volza) و
`TradeMap Companies Database - Verified & Completed.xlsx` (۱۷۰ شرکت، منبع ITC Trade Map).

**تفاوت کلیدی با فایل‌های قبلی:** همه‌ی فایل‌های `لیست شرکتها/` قبلی مخصوص یک کشور بودند
(یک فایل = یک کشور، در `SOURCES` مشخص می‌شد). این دو فایل **جهانی**اند — شرکت‌های چند ده
کشور مختلف در یک فایل، بدون ستون «کشور» جداگانه؛ فقط داخل ستون آدرس («…، Praha, Czech
Republic») کشور آمده بود.

**چه ساخته شد:**
1. `ingest_companies.py` یک مسیر دوم گرفت: `GLOBAL_SOURCES` + `resolve_country_from_address()`
   که آخرین بخش قابل‌تشخیص آدرس را با `country_name_map.json` (همان نگاشتی که
   `ingest_trade_map.py` استفاده می‌کند) به نام فارسی/ISO2 کشور تبدیل می‌کند. الگوریتم از
   انتهای آدرس به عقب می‌گردد تا کد پستی یا جزئیات شعبه («Canada, H9X 3P1») را رد کند و به
   خودِ اسم کشور برسد؛ برای کشورهایی که در نگاشت با نام رسمی ISO ثبت شده‌اند («Viet Nam» نه
   «Vietnam»، «Korea, Republic of» نه «South Korea») یک جدول کوچک استثنا هم اضافه شد.
2. **بررسی صحت قبل از ادغام (نه فقط اعتماد به فایل):**
   - تطبیق با ۱۹۲ شرکت موجود بر اساس نام دقیق: صفر برخورد.
   - تطبیق بر اساس دامنه‌ی وب‌سایت اول اشتباه از آب درآمد — Brenntag/IMCD/Manuchar شرکت‌های
     چندملیتی‌اند که هر شعبه‌ی کشوری‌شان از همان دامنه‌ی مادر استفاده می‌کند (مثلاً
     «Brenntag Química Brasil» که از قبل برای برزیل ثبت بود، با «BRENNTAG SPA» ایتالیا هیچ
     ربطی ندارد جز نام شرکت مادر). این معیار کنار گذاشته شد.
   - فقط ردیف‌های **کاملاً یکسان** (هر ۸ ستون عیناً تکراری) در همان فایل حذف شدند — ۳ مورد.
     ردیف‌هایی با نام یکسان ولی آدرس/تلفن متفاوت (مثلاً ۱۹ شعبه‌ی مختلف Brenntag SPA در
     شهرهای گوناگون ایتالیا) نگه داشته شدند، چون برای بازاریابی هرکدام نقطه‌تماس واقعی
     جداگانه‌ای‌اند.
3. هر ۱۸۲ ردیف معتبر (بعد از حذف ۳ ردیف کاملاً تکراری و چند ردیف کاملاً خالی) کشورشان با
   موفقیت تشخیص داده شد — صفر هشدار «کشور نامشخص».

**نتیجه:** بانک شرکت‌ها از ۱۹۲ به **۳۷۱** رسید، با ۲۹ کشور تازه (عمدتاً اروپا: ایتالیا ۳۵،
لهستان ۲۱، رومانی ۱۲، فرانسه ۱۰… به‌علاوه‌ی مصر، چین، هند، ترکیه، ویتنام، امارات و چند مورد
دیگر). هر کشور تازه یک صفحه‌ی مستقل در `/countries` گرفت (خودکار، چون `getCountries()` از
روی داده‌ی واقعی کشورها را می‌سازد، نه فهرست دستی).

**فیلد جدید:** هر رکورد از منبع جهانی یک `source_note` دارد («Volza (راستی‌آزمایی‌شده)» یا
«ITC Trade Map (راستی‌آزمایی‌شده)») تا در آینده معلوم باشد این شرکت از کدام مسیر آمده —
برخلاف فایل‌های تک‌کشوری قدیمی که اصلاً به تأیید بیرونی نیاز نداشتند.

**فایل‌ها:** `scripts/ingest_companies.py`، `data/companies.json`، `README.md`،
`گزارش/لیست شرکتها/` (دو فایل تازه)

**نکته برای آینده:** این پایپ‌لاین حالا برای هر فایل جهانی مشابه (نه فقط جوش شیرین) کار
می‌کند — کافی است فایل را با همان اسکیمای هشت‌ستونی در `گزارش/لیست شرکتها/` بگذاری و به
`GLOBAL_SOURCES` اضافه کنی؛ نیازی به تغییر منطق تشخیص کشور نیست.

---

### ۲۰۲۶-۰۸-۲۵ (۱۱) — تفکیک مبدأ‌به‌مبدأ واردات (فایل ITC ارمنستان)

**ورودی:** فایل «بازارهای تأمین‌کننده‌ی واردات ارمنستان ۲۰۲۵» — همان فایلی که در یادداشت
افزودن ارمنستان به‌عنوان «استفاده‌نشده» علامت خورده بود.

**چرا مهم است:** تا امروز «شرکای تجاری» هر کشور را هوش مصنوعی از **متن گزارش‌ها** بیرون
می‌کشید — سه مورد اول، بدون عدد دقیق. این فایل همان اطلاعات را مستقیم از منبع رسمی و برای
**کل فهرست** می‌دهد، به‌علاوه‌ی دو ستون که در هیچ منبع دیگری نداشتیم: **قیمت واحد هر مبدأ**
و **تعرفه‌ی گمرکی که کشور واردکننده بر آن مبدأ اعمال می‌کند**.

آن ستون تعرفه یک ادعای گزارش تحلیلی را با عدد رسمی تأیید کرد: **روسیه ۰٪، ایران ۵٪** — یعنی
همان مزیت عضویت روسیه در اتحادیه‌ی اقتصادی اوراسیا که در گزارش توضیح داده شده بود.

**چه ساخته شد:**
1. `scripts/ingest_import_suppliers.py` — پایپ‌لاین **عمومی**، نه مخصوص ارمنستان. نام کشور
   واردکننده و سال را از خود نام فایل می‌خواند، پس برای کشور بعدی فقط باید فایلش را در
   `گزارش/تجارت جهانی/` گذاشت؛ کد دست نمی‌خورد.
2. `data/import_suppliers.json` — برای ارمنستان ۸ تأمین‌کننده با ارزش، سهم، حجم، قیمت واحد،
   نرخ رشد ۵ ساله و تعرفه.
3. `SupplierBreakdown.jsx` — کارت «کشورهای مبدأ» طبق طرح مرجع: پرچم + نام + ارزش + نوار سهم
   + رشد سالانه، و زیرش سهم/قیمت واحد/تعرفه. ردیف ایران عمداً برجسته می‌شود (پس‌زمینه‌ی مسی)
   چون در هر بازار، اولین سؤال «جای ما کجاست» است.

**تصویر بازار ارمنستان که حالا روی صفحه است:** ایران ۵۳.۳٪ سهم با ۳۳۳ دلار بر تن و رشد
سالانه‌ی +۱۴٪ · روسیه ۲۸.۷٪ با ۳۱۳ دلار و **تعرفه‌ی صفر** ولی رشد −۱۳٪ · ترکیه ۷.۱٪ ولی با
۲٬۳۸۵ دلار بر تن (بسته‌بندی خرده‌فروشی، نه فله) · چین ۴.۸٪ · آلمان ۴.۶٪ با روند −۵۹٪.

یعنی ایران در این بازار **هم رتبه‌ی اول است هم در حال رشد**، در حالی که رقیب نزدیکش (روسیه)
با وجود تعرفه‌ی صفر در حال عقب‌نشینی است — دقیقاً برعکس تصویر جهانی که در آن صادرات ایران
−۱۶.۲٪ سالانه است.

**فایل‌ها:** `scripts/ingest_import_suppliers.py` (جدید)، `data/import_suppliers.json` (جدید)،
`web/app/components/SupplierBreakdown.jsx` (جدید)، `web/lib/data.js`،
`web/app/countries/[country]/page.jsx`، `README.md`، `گزارش/تجارت جهانی/` (فایل ارمنستان)

---

### ۲۰۲۶-۰۸-۲۵ (۱۰) — بازطراحی ظاهری (۴): یکدست‌سازی سربرگ‌ها و نوار شناسنامه‌ی کشور

**۱) ناهماهنگی که خودم گذاشته بودم، رفع شد.** در پاس‌های قبلی فقط بعضی صفحات سربرگشان به
بوم تیره منتقل شده بود؛ صفحات کشورها، شرکت‌ها و نمایشگاه‌ها هنوز عنوان و جست‌وجویشان را
**داخل کارت سفید** داشتند. حالا هر سه به `PageHeader` منتقل شدند و کنترل‌های فیلترشان
(جست‌وجو و انتخاب کشور) به‌شکل قرص‌های تیره در سمت چپ سربرگ نشستند — همان الگوی طرح.

نتیجه: هر صفحه‌ی سایت حالا یک ریتم واحد دارد — سربرگ روی بوم، محتوا داخل کارت.

**۲) نوار شناسنامه‌ی کشور** (`CountryStatStrip.jsx`) از روی `ui/03-country.png` ساخته شد:
نوار تیره‌ی افقی با پرچم گرد و نام کشور در سمت راست، و تا شش ستون آمار در ادامه. چرا تیره
و نه کارت سفید مثل بقیه: این نوار «شناسنامه» است نه یکی از بخش‌های محتوا؛ تیره‌بودنش آن را
از کارت‌های زیرش جدا می‌کند و پرچم هم بدون تداخل رنگی داخلش می‌نشیند.

آمارها همه از داده‌ی واقعی می‌آیند و هرکدام که نباشد ستونش حذف می‌شود. برای ارمنستان:
ارزش واردات ۰.۴۴ میلیون دلار · حجم ۱٬۱۸۰ تن · قیمت میانگین ۳۶۹ دلار/تن · رشد سالانه‌ی
صادرات −۵۷.۴٪ · ۱۳ شرکت · ۴ نمایشگاه.

**۳) کارت‌های صفحه‌ی کشورها** هم به کلاس `card` و `card-hover` منتقل شدند (جا مانده بودند).

**یک درس فنی:** تلاش اول برای جابه‌جایی سربرگ‌ها با اسکریپت انجام شد و JSX را شکست (دو
خطای «Expected '</'»). ساختار تودرتوی JSX با جایگزینی متنی قابل‌اعتماد نیست؛ اصلاح دستی
انجام شد. برای تغییرات ساختاری JSX، ویرایش هدفمند بهتر از اسکریپت است.

**بررسی:** هر ۱۲ مسیر با سرور نسخه‌ی نهایی تست شد — همه ۲۰۰، و محتوای نوار شناسنامه در
خروجی صفحه‌ی ارمنستان تأیید شد.

**فایل‌ها:** `web/app/components/CountryStatStrip.jsx` (جدید)،
`web/app/countries/CountriesClient.jsx`، `web/app/countries/[country]/page.jsx`،
`web/app/companies/CompaniesClient.jsx`، `web/app/exhibitions/ExhibitionsClient.jsx`

---

### ۲۰۲۶-۰۸-۲۵ (۹) — بازطراحی ظاهری (۳): سیستم کارت، کارت‌های KPI و گرید اخبار

**بازخورد کاربر:** «سایدبار خوب شد ولی هنوز طراحی عوض نشده — رنگ‌بندی و کادرها را مثل طرح
کن.» درست بود: پاس قبلی فقط بوم و ناوبری را عوض کرده بود، خودِ کارت‌ها هنوز شکل قدیمی
داشتند (`rounded-xl` + حاشیه‌ی خاکستری + سایه‌ی تک‌لایه).

**۱) سیستم کارت مشترک.** کلاس `.card` در `globals.css` تعریف شد و **۲۷ کارت در ۱۷ فایل**
به آن مهاجرت کردند. جزئیات از روی طرح: گردی ۱۶ پیکسل (نه ۱۲)، حاشیه‌ی تقریباً نامرئی، و
**سایه‌ی دولایه** — یک لایه‌ی نزدیک برای لبه و یک لایه‌ی پخش برای جداشدن از بوم تیره. روی
زمینه‌ی تیره، سایه‌ی تک‌لایه‌ی پیش‌فرض Tailwind عملاً دیده نمی‌شود و کارت‌ها «شناور» به‌نظر
نمی‌رسیدند — همین بود که باعث می‌شد تغییر رنگ بوم به‌تنهایی کافی نباشد.

چرا کلاس مشترک به‌جای تکرار کلاس‌های Tailwind: شکل کارت عنصر اصلی هویت بصری این سایت است؛
وقتی در ۲۷ جا تکرار شده باشد، اولین تغییر طراحی یعنی ۲۷ نقطه که یکی‌شان جا می‌ماند.

**۲) کلاس `.chip`** برای دکمه‌های فیلتر روی بوم تیره، با حالت فعال مسی.

**۳) کارت‌های KPI بازنویسی شد** — ساختارش حالا عیناً طرح مرجع است: برچسب (راست) و نشان
درصد تغییر (چپ) در یک ردیف، عدد خیلی درشت با واحد، خط جداکننده، و در پایین یادداشت (راست)
با **نمودار خطی کوچک** (چپ). نمودار با SVG دستی نوشته شد نه Recharts: برای پنج نقطه بدون
محور و راهنما، آوردن موتور نمودار هم حجم می‌آورد هم در RTL دردسر محور دارد.

تغییر محتوایی مهم: عدد اصلی کارت‌های رقبا از «درصد رشد» به **ارزش صادرات** تغییر کرد و
درصد رشد به نشان بالای کارت رفت — قبلاً هر دو یک عدد را نشان می‌دادند.

**۴) صفحه‌ی اخبار به گرید سه‌ستونی رفت** (طبق `ui/03-news.png`): چیپ‌های فیلتر موضوعی روی
بوم، و کارت‌هایی با چیپ موضوع + زمان نسبی + تیتر + خلاصه + حقایق کلیدی + منابع.

**یک انحراف عمدی از طرح:** در طرح مرجع داخل هر کارت خبر یک نمودار میله‌ای کوچک هست، ولی
داده‌ی نمونه‌ی خودش را می‌کشد (v0 هم در لاگش نوشته بود این نمودار خراب رندر می‌شود). خبرهای
ما سری زمانی ندارند — هر خبر یک رویداد است، نه یک روند. پس به‌جای نمودار تزئینی، **حقایق
کلیدی** همان خبر که ربات واقعاً استخراج کرده نمایش داده می‌شود.

**بررسی:** هر ۱۳ مسیر با سرور نسخه‌ی نهایی تست شد — همه ۲۰۰، و شمارش کلاس `card` در خروجی
هر صفحه تأیید شد (داشبورد ۷، ترکیه ۲۱، اخبار ۲۲).

**فایل‌ها:** `web/app/globals.css`، `web/app/components/KpiRow.jsx`،
`web/app/news/NewsClient.jsx`، و ۱۷ فایل دیگر برای مهاجرت کلاس کارت

---

### ۲۰۲۶-۰۸-۲۵ (۸) — بازطراحی ظاهری (۲): بوم تیره و سربرگ مشترک، بر پایه‌ی طرح v0

**ورودی:** کاربر یک طرح در v0 ساخت و چهار اسکرین‌شات داد (`ui/01-dashboard.png` تا
`ui/03-country.png`). لینک خود v0 پشت لاگین بود و WebFetch فقط صفحه‌ی ورود را برگرداند؛
کد هم قابل کپی نبود، پس از روی تصاویر کار شد.

**مهم‌ترین یافته: پالت طرح دقیقاً پالت خودمان است.** نمونه‌برداری پیکسلی از تصاویر:
`#0B2027` = `petrol-900`، `#123742` = `petrol-700`، `#DA8C42` = `copper-400`. یعنی هیچ
مهاجرت برندی لازم نبود.

**تفاوت ساختاری اصلی:** بوم صفحه **تیره** است و کارت‌ها سفید روی آن می‌نشینند (قبلاً بوم
روشن بود). نکته‌ی ظریف: سایدبار (`petrol-700`) از بوم (`petrol-900`) **روشن‌تر** است، نه
تیره‌تر — همین باعث می‌شود ستون ناوبری جلو بیاید.

**چه شد:**
1. بوم به `petrol-900` رفت و فوتر هم‌رنگ شد.
2. سایدبار به `petrol-700`، و نشانگر آیتم فعال از نقطه‌ی کوچک به **نوار مسی لبه‌ی راست**
   تغییر کرد (همان الگوی طرح).
3. `PageHeader.jsx` ساخته شد: عنوان + زیرعنوان + breadcrumb اختیاری + جای دکمه‌ی کنش،
   با خط جداکننده‌ی پایین. صفحه‌ی کشور اولین استفاده‌کننده‌ی breadcrumb است
   (داشبورد / کشورها / نام کشور).
4. سربرگ داشبورد، گزارش‌های هوشمند، رقبا، جست‌وجو و صفحه‌ی کشور به این کامپوننت منتقل شد.

**ریسک اصلی و نحوه‌ی مهارش:** با تیره‌شدن بوم، هر متنی که بیرون کارت سفید بود
نامرئی می‌شد. به‌جای چشمی گشتن، با یک اسکریپت همه‌ی صفحات اسکن شدند تا هر متنی که قبل از
اولین کارت می‌آید و کلاس `text-slate-*` دارد پیدا شود. سه لینک بازگشت (رقبا، نمایشگاه،
گزارش) و سربرگ صفحه‌ی `/ask` این‌طور پیدا و اصلاح شدند. یک مورد (`CountriesClient`)
مثبت کاذب بود چون کل صفحه‌اش داخل کارت سفید است.

**بررسی:** هر ۱۲ مسیر با سرور نسخه‌ی نهایی تست شد — همه ۲۰۰.

**قدم بعدی:** کارت‌های KPI با نمودار کوچک و نشان درصد (طبق طرح)، گرید سه‌ستونی اخبار با
چیپ فیلتر، و نوار آمار تیره‌ی صفحه‌ی کشور.

**فایل‌ها:** `web/app/components/PageHeader.jsx` (جدید)، `web/app/components/Sidebar.jsx`،
`web/app/layout.js`، `web/app/page.js`، `web/app/reports/page.jsx`،
`web/app/competitors/page.jsx`، `web/app/competitors/[id]/page.jsx`،
`web/app/countries/[country]/page.jsx`، `web/app/exhibitions/[id]/page.jsx`،
`web/app/reports/[id]/page.jsx`، `web/app/ask/AskClient.jsx`، `ui/` (تصاویر مرجع طرح)

---

### ۲۰۲۶-۰۸-۲۵ (۷) — شروع بازطراحی ظاهری: سایدبار + ردیف آمار کلیدی داشبورد

**زمینه:** ارائه به مدیرعامل انجام شد و بازخورد مثبت بود؛ حالا نوبت کار روی ظاهر است.
مبنا، همان چهار الگویی است که از طرح کانوا استخراج شد (یادداشت ۲۳ اوت).

**۱) ناوبری: از نوار افقی به سایدبار.** ده آیتم در یک نوار افقی، در نمایشگر کوچک به چند
سطر می‌شکست، ترتیب بصری‌اش به هم می‌ریخت، و هیچ نشانه‌ای از «کدام صفحه باز است» نداشت.
`web/app/components/Sidebar.jsx` ساخته شد:

- ستون ثابت سمت راست (چون کل سایت RTL است)، با `sticky` تا در صفحات بلند مثل کشورها و
  شرکت‌ها منو از دست نرود.
- گروه‌بندی موضوعی به‌جای فهرست تخت: بازار (کشورها، رقبا، ترانزیت) · محتوا (گزارش‌ها،
  آرشیو، اخبار) · بانک اطلاعات (شرکت‌ها، نمایشگاه‌ها) · و «جست‌وجو و پرسش» جدا در پایین.
- حالت فعال با `usePathname` — نقطه‌ی مسی + پس‌زمینه‌ی کم‌رنگ.
- زیر breakpoint لارج به نوار بالا + کشوی بازشونده تبدیل می‌شود.
- آیکون‌ها inline SVG هستند؛ ده آیکون ساده ارزش افزودن یک وابستگی جدید را نداشت.

**۲) ردیف آمار کلیدی داشبورد.** `KpiRow.jsx` با چهار کارت: قیمت پایه‌ی مرجع خودمان، و
رشد سالانه‌ی صادرات ترکیه (+۱۳.۵٪)، چین (+۱۰.۳٪) و ایران (−۱۶.۲٪). هر کارت رقیب به
صفحه‌ی خودش لینک است، پس نقطه‌ی شروع ناوبری هم هست نه فقط تزئین. اگر داده‌ای نبود، کارتش
اصلاً ساخته نمی‌شود (به‌جای نمایش صفر یا خط تیره‌ی بی‌معنی).

**دو ایراد که در بازبینی خروجی گرفته شد:**
- درصد دوبار نمایش داده می‌شد (یک‌بار به‌عنوان مقدار، یک‌بار در نشان روند). حالا فقط فلش
  جهت می‌آید و خودِ عدد یک‌بار.
- سال‌های ۲۰۲۱–۲۰۲۵ با ارقام لاتین رندر می‌شدند در حالی که بقیه‌ی صفحه فارسی بود.
  `toLocaleString` هم به‌تنهایی جواب نمی‌داد چون برای سال جداکننده‌ی هزارگان می‌گذارد
  (۲٬۰۲۱)؛ با یک تابع کوچک `faDigits` حل شد.

**بررسی:** هر ۱۱ مسیر اصلی سایت با سرور نسخه‌ی نهایی تست شد — همه ۲۰۰ و همه سایدبار را
دارند.

**قدم‌های بعدی این مسیر (هنوز انجام نشده):** گرید کارت‌های خبر با نمودار کوچک در صفحه‌ی
اخبار، نوارهای متناوب تیره/روشن برای ریتم صفحه، و پاس تایپوگرافی و فاصله‌گذاری.

**فایل‌ها:** `web/app/components/Sidebar.jsx` (جدید)، `web/app/components/KpiRow.jsx`
(جدید)، `web/app/layout.js`، `web/app/page.js`

---

### ۲۰۲۶-۰۸-۲۵ (۶) — روند ۵ ساله‌ی صادرات و نرخ رشد سالانه (کشورها + رقبا)

**ورودی:** فایل سری زمانی ITC (ارزش صادرات هر کشور، ۲۰۲۱ تا ۲۰۲۵). فایل دوم («۲۰۲۵ (2)»)
با نسخه‌ی موجود در مخزن **هش یکسان** داشت، پس چیزی از آن وارد نشد.

**چه ساخته شد:**

1. `scripts/ingest_trade_map.py`: پارسر سوم برای فایل سری زمانی (جدول ششم، سرستون‌های
   «Exported value in YYYY»). سال‌ها از خود سرستون خوانده می‌شوند، پس اگر سال بعد یک ستون
   اضافه شود اسکریپت بدون تغییر کار می‌کند.
2. کلید `export_trend` به هر کشور در `data/trade_map_2025.json` اضافه شد: سال‌ها، مقادیر،
   **نرخ رشد سالانه‌ی مرکب (CAGR)** و درصد تغییر کل. ۱۲۶ کشور از ۲۰۳ کشور روند دارند.
3. کامپوننت مشترک `web/app/components/ExportTrend.jsx` با دو حالت نمایش (کارت و سربرگ)،
   به‌همراه یک sparkline ساده‌ی ۵ ستونی بدون کتابخانه‌ی نمودار.
4. نمایش در سه جا: کارت‌های صفحه‌ی کشورها، بخش آمار تجارت جهانی در صفحه‌ی هر کشور، و
   سربرگ صفحه‌ی هر رقیب.

**چرا CAGR و نه درصد تغییر کل:** این بازار نوسانی است و مقایسه‌ی صرفِ اولین و آخرین سال،
جهش‌های میانی را پنهان می‌کند (بلغارستان: ۴٬۸۶۱ → ۲۶٬۴۷۸ → ۳۸٬۳۸۸ → ۴۳٬۵۹۱ → ۳۲٬۲۱۶).
نرخ سالانه‌ی مرکب رشد را به زبان «سالی چند درصد» می‌گوید که بین کشورها قابل مقایسه است.
هر دو عدد ذخیره و نمایش داده می‌شوند. اگر سال پایه صفر باشد CAGR بی‌معنی است و `null`
می‌ماند — چیزی از خودمان نمی‌سازیم.

**یافته‌های قابل‌توجه (برای ارائه):**

| کشور | ۲۰۲۱ → ۲۰۲۵ (هزار دلار) | رشد سالانه |
|---|---|---|
| ترکیه | ۱۰۳٬۱۳۲ → ۱۷۰٬۸۵۵ | **+۱۳.۵٪** |
| چین | ۱۲۷٬۴۷۳ → ۱۸۸٬۷۴۳ | **+۱۰.۳٪** |
| کل جهان | ۸۴۷٬۶۷۶ → ۱٬۰۱۰٬۸۰۰ | +۴.۵٪ |
| آلمان | ۱۵۰٬۶۵۲ → ۸۶٬۱۷۷ | −۱۳.۰٪ |
| **ایران** | ۳٬۴۶۱ → ۱٬۷۰۴ | **−۱۶.۲٪** |

یعنی هر دو رقیب اصلی دو تا سه برابر سریع‌تر از خود بازار جهانی رشد کرده‌اند، در حالی که
صادرات ایران در همان دوره نصف شده است. این تک‌عدد، قوی‌ترین استدلال موجود در پلتفرم برای
ضرورت همین پروژه است.

**فایل‌ها:** `scripts/ingest_trade_map.py`، `data/trade_map_2025.json`،
`web/app/components/ExportTrend.jsx` (جدید)، `web/app/countries/CountriesClient.jsx`،
`web/app/countries/[country]/page.jsx`، `web/app/competitors/[id]/page.jsx`،
`گزارش/تجارت جهانی/` (فایل سری زمانی)

---

### ۲۰۲۶-۰۸-۲۵ (۵) — آمار صفحه‌ی رقبا از ۲۰۲۴ به ۲۰۲۵ رفت (و دیگر دستی منجمد نمی‌ماند)

**درخواست:** آمار کشورها باید ۲۰۲۵ باشد؛ عدد ۲۰۲۴ دو سال گذشته و ارزش ندارد. فایل جدید
ITC (فهرست صادرکنندگان ۲۰۲۵) هم داده شد.

**اول یک بررسی:** فایل جدید با فایلی که از قبل در `گزارش/تجارت جهانی/` بود **بایت‌به‌بایت
هم‌ارز** بود — همان ۱۰۴ ردیف و همان اعداد (ترکیه ۱۷۰٬۸۵۵ هزار دلار، چین ۱۸۸٬۷۴۳). پس
`data/trade_map_2025.json` از قبل به‌روز بود و صفحه‌ی کشورها هم درست ۲۰۲۵ را نشان می‌داد.
مشکل جای دیگری بود: **صفحه‌ی رقبا** اعدادش را از `competitors.json` می‌گرفت که دستی از روی
گزارش‌های تحلیلی ۲۰۲۴ نوشته شده بود.

**رفع ریشه‌ای (نه فقط عوض‌کردن عدد):** آمار تجاری سربرگ صفحه‌ی رقیب و کارت‌های فهرست رقبا
حالا **در زمان رندر از `trade_map_2025.json` ساخته می‌شوند** — همان منبعی که صفحه‌ی کشورها
استفاده می‌کند. یعنی سال بعد با یک بار اجرای `ingest_trade_map.py` همه‌جا خودکار به‌روز
می‌شود و دیگر لازم نیست کسی یادش باشد عدد را دستی عوض کند.

نتیجه‌ی روی صفحه:

| | ترکیه | چین |
|---|---|---|
| حجم صادرات ۲۰۲۵ | ۵۳۶٬۲۹۹ تن | ۱٬۰۰۶٬۰۶۳ تن |
| ارزش صادرات ۲۰۲۵ | ۱۷۰.۹ میلیون دلار | ۱۸۸.۷ میلیون دلار |
| سهم بازار جهانی ۲۰۲۵ | ۱۶.۹٪ | ۱۸.۷٪ |
| قیمت متوسط صادراتی ۲۰۲۵ | ۳۱۹ دلار/تن | ۱۸۸ دلار/تن |

(قبلاً: ترکیه ۵۵۹٬۳۶۶ تن و ۱۵۲.۷ تا ۱۶۱ میلیون دلار در ۲۰۲۴؛ چین ۲۲۴ میلیون دلار و ~۲۰٪.)

**در `competitors.json` فقط آمارهایی ماندند که در ITC نیستند:** هزینه‌ی نقدی تولید ترکیه
(۸۳.۲ دلار، ۲۰۲۴ — از گزارش مالی WE Soda) و زمان حمل دریایی چین. هر دو با سالشان برچسب
خورده‌اند، چون جدیدترین داده‌ی موجودشان همان است و وانمود نمی‌کنیم ۲۰۲۵ است.

**متن معرفی چین هم بازنویسی شد** — قبلاً با «در سال ۲۰۲۴ با صادرات ۲۲۴ میلیون دلاری» شروع
می‌شد که با آمار بالای همان صفحه تناقض پیدا می‌کرد.

**یک چیز عمداً ۲۰۲۴ ماند:** نمودار «۱۰ مقصد اصلی صادرات ترکیه». فایل ITC تفکیک مقصدبه‌مقصد
**ندارد** (فقط سرجمع جهانی هر کشور را دارد)؛ آن داده از گزارش تحلیلی ترکیه می‌آید. دلیلش
حالا در زیرنویس خود نمودار نوشته شده تا شبیه جاافتادگی به نظر نرسد.

**فایل‌ها:** `data/competitors.json`، `web/app/competitors/[id]/page.jsx`،
`web/app/competitors/page.jsx`، `web/app/components/CompetitorCharts.jsx`

---

### ۲۰۲۶-۰۸-۲۵ (۴) — حذف آمار «سهم از واردات عراق» از سربرگ صفحه‌ی رقبا

**درخواست:** این آمار در سربرگ (بخش اول) صفحه‌ی هر رقیب می‌آمد و از قبل مانده بود؛ برجسته‌کردن
یک کشور خاص در خلاصه‌ی کلی رقیب موضوعیت ندارد.

**چه شد:** فیلد مربوطه از `headline_stats` هر دو رقیب در `data/competitors.json` حذف شد
(ترکیه: «سهم ترکیه از واردات عراق»، چین: «سهم از واردات عراق (۲۰۲۴)»). هر دو حالا سه آمار
دارند: ترکیه حجم و ارزش صادرات ۲۰۲۴ و هزینه‌ی نقدی تولید؛ چین ارزش صادرات، سهم بازار جهانی
و زمان حمل دریایی.

**یک اصلاح جانبی:** گرید سربرگ روی `lg:grid-cols-4` ثابت بود و با سه آمار یک خانه‌ی خالی
می‌ماند. حالا تعداد ستون با تعداد آمار هماهنگ می‌شود.

**دست‌نخورده ماند:** بخش تحلیلی اختصاصی «بازار عراق — مهم‌ترین بازار مرزی ترکیه» پایین صفحه‌ی
ترکیه سر جایش است (با تفکیک قیمت مرحله‌به‌مرحله). آن‌جا عراق موضوعیت دارد چون یک تحلیل
مسیرمحور است، نه یک آمار عمومی در خلاصه‌ی رقیب.

**فایل‌ها:** `data/competitors.json`، `web/app/competitors/[id]/page.jsx`

---

### ۲۰۲۶-۰۸-۲۵ (۳) — رفع خطای ۴۰۳ «پاسخ هوشمند» روی اجرای محلی

**علامت:** روی `localhost:3000` سؤال «میزان صادرات چین به کنیا» خطای «دسترسی به سرویس هوش
مصنوعی مسدود شد (۴۰۳)» گرفت.

**علت ریشه‌ای — نه کلید، نه کد، بلکه پراکسی:** Groq و Gemini از ایران مستقیم در دسترس نیستند.
پراکسی محلی روی سیستم فعال بود (`127.0.0.1:10808`) و تست مستقیم از ترمینال هم جواب می‌داد،
ولی بررسی نشان داد `HTTPS_PROXY` **فقط داخل ترمینال** تنظیم شده و در متغیرهای محیطی ویندوز
(User و Machine هر دو خالی) وجود ندارد. پس پروسه‌ی Node که با «اجرای-سایت.bat» بالا می‌آید
هیچ پراکسی‌ای نمی‌دید و درخواستش مستقیم می‌رفت و بلاک می‌شد.

**چه شد:**
1. `resolveProxyUrl()` در `groqFetch.js`: اول متغیر محیطی، و اگر نبود خط `HTTPS_PROXY` از
   فایل `.env` ریشه‌ی پروژه — دقیقاً همان الگویی که برای کلیدهای API استفاده می‌شود.
   `geminiFetch.js` هم همین تابع را وارد می‌کند تا دو نسخه‌ی جدا از هم دِریفت نکنند.
2. یک خط `HTTPS_PROXY=http://127.0.0.1:10808` به `.env` اضافه شد (این فایل در `.gitignore`
   است و کامیت نمی‌شود — روی سرور/گیت‌هاب اکشنز که دسترسی مستقیم دارند، نبودش مشکلی
   ایجاد نمی‌کند چون فقط وقتی پیدا شود استفاده می‌شود).
3. شرط موتور پشتیبان از «فقط ۴۲۹» به «۴۲۹ یا ۴۰۳ یا ۵xx» گسترش یافت — هر سه یعنی
   «Groq الان در دسترس نیست». خطای ۴۱۳ عمداً fallback نمی‌گیرد، چون با موتور دیگر هم
   همان مشکل تکرار می‌شود.

**تست قطعی:** سرور با `env -u HTTPS_PROXY` بالا آورده شد (یعنی دقیقاً شرایط bat، بدون هیچ
متغیر پراکسی) و هر دو سؤال جواب درست گرفتند: صادرات چین به کنیا ۳٬۶۰۳.۱ تن (۳۲.۸٪ بازار) و
هزینه‌ی تولید ترکیه با تفکیک درست دو روش (۸۳.۲ دلار استخراج محلول در برابر سنتتیک).

**فایل‌ها:** `web/lib/groqFetch.js`، `web/lib/geminiFetch.js`، `web/app/api/ask/route.js`،
`.env` (کامیت نمی‌شود)

**نکته برای آینده:** اگر روزی پورت پراکسی عوض شد یا روی سیستم دیگری اجرا شد، فقط همان خط
`HTTPS_PROXY` در `.env` باید به‌روز شود؛ نه کد و نه bat دست نمی‌خورد.

---

### ۲۰۲۶-۰۸-۲۵ (۲) — رفع تناقض بصری نمودارهای صفحه‌ی ترکیه

**گزارش کاربر:** در صفحه‌ی ترکیه تناقض هست — اتی سودا و کازان سودا با استخراج معدنی تولید
می‌کنند و شیشه‌جام با روش صنعتی، و روش اتی/کازان کم‌هزینه‌تر است.

**بررسی داده: داده درست بود.** در `data/competitors.json` هر سه تولیدکننده درست ثبت شده‌اند —
کازان سودا و اتی سودا با `tech_type: "solution"` (استخراج محلول ترونای طبیعی) و سودا سانایی
(شیشه‌جام) با `tech_type: "synthetic"` (فرآیند سُلوِه)؛ هزینه‌ها هم ۸۳.۲ دلار در برابر ۱۵۰ تا ۱۹۰
دلار. کارت‌های تولیدکننده هم از قبل درست رنگ‌بندی شده بودند.

**ایراد واقعی در خود نمودارها بود:**

1. **رنگ یک دسته در دو نمودار کنار هم فرق داشت.** نمودار ظرفیت، «استخراج محلول» را مسی
   می‌کشید ولی نمودار هزینه همان روش را **قرمز**. بیننده دو رنگ متفاوت برای یک چیز می‌دید.
2. **قرمز پیام را وارونه می‌رساند.** در عرف نمودار قرمز یعنی «خطر/بد»، در حالی که استخراج
   محلول ارزان‌ترین روش و نقطه‌ی قوت رقیب است.
3. **برچسب‌ها روی میله‌ها می‌افتادند.** نمودار هزینه نواری افقی بود و در چیدمان راست‌به‌چپ،
   برچسب‌های بلند محور عمودی روی خود میله‌ها رندر و بریده می‌شدند.

**چه شد:**
- ثابت مشترک `METHOD_COLOR` اضافه شد: `solution` همیشه مسی، `synthetic` همیشه نفتی — در
  همه‌ی نمودارهای صفحه. رنگ فقط «روش تولید» را کد می‌کند و قضاوت در متن زیر نمودار می‌آید.
- رنگ قرمز (`DANGER`) کاملاً حذف شد (دیگر هیچ‌جا استفاده نمی‌شد).
- نمودار هزینه از نواری افقی به **ستونی** تبدیل شد — هم مشکل همپوشانی برچسب حل شد، هم
  هم‌شکل نمودار ظرفیت کناری‌اش شد. مقدار هر ستون بالای آن نوشته می‌شود و tooltip نام شرکت
  مربوط به هر روش را هم نشان می‌دهد.
- زیرنویس هر دو نمودار صریح شد: کدام شرکت با کدام روش، و کدام ارزان‌تر.

**فایل‌ها:** `web/app/components/CompetitorCharts.jsx`

---

### ۲۰۲۶-۰۸-۲۵ — پرچم رقبا + بازرسی کامل پیش از ارائه به مدیرعامل (یک باگ جدی پیدا و رفع شد)

**زمینه:** ارائه‌ی پلتفرم به مدیرعامل در همین روز. دو کار خواسته شد: پرچم کشورهای رقیب به‌عنوان
پس‌زمینه (مثل صفحه‌ی کشورها)، و یک مرور کلی برای اطمینان از نبود خطا.

**۱) پرچم رقبا.** کارت‌های صفحه‌ی `/competitors` و سربرگ صفحه‌ی هر رقیب حالا پرچم کشور را
به‌عنوان پس‌زمینه دارند، با همان گرادیان تیره‌ی صفحه‌ی کشورها. کد دوحرفی کشور از همان منبع
(`trade_map_2025.json`) خوانده می‌شود، نه یک فهرست دستی جدید — تا اگر اصلاح شد، هر دو صفحه
با هم درست بمانند. تأیید شد: ترکیه `tr`، چین `cn`.

**۲) بازرسی داده — پاک.** همه‌ی JSONها سالم، هر ۱۱ گزارش با فایل اصلی و فایل parsed موجود،
هیچ گزارش بدون محتوا، هر ۲۸ نمایشگاه دارای تاریخ، هر ۱۹۲ شرکت حداقل یک راه تماس دارد، صفر
مورد «قدمی»، و هر پنج داده‌ی روزانه تا ۲۴ اوت به‌روز (اجرای خودکار ۲۴ اوت با کدهای اصلاح‌شده
موفق بوده).

**۳) بازرسی زنده — همه‌ی ۱۴ مسیر سایت ۲۰۰ برگرداندند.** سرور نسخه‌ی نهایی بالا آمد و تک‌تک
صفحه‌ها با curl تست شدند (بدون باز کردن مرورگر).

**۴) باگ جدی که پیدا شد: «پاسخ هوشمند» عملاً برای سؤال‌های مهم کار نمی‌کرد.**

مسیر `/api/ask` تا ۸ قطعه × ۳۵۰۰ کاراکتر (~۲۸٬۰۰۰ کاراکتر ≈ بیش از ۱۱٬۰۰۰ توکن) به Groq
می‌فرستاد، در حالی که سقف سرویس رایگان ۸۰۰۰ توکن در دقیقه است و Groq «پرامپت + max_tokens»
را پیش از اجرا با سقف می‌سنجد. یعنی **هر سؤالی که به متن گزارش‌های بلند می‌خورد با خطای ۴۱۳
شکست می‌خورد**؛ فقط سؤال‌هایی جواب می‌گرفتند که به قطعه‌های کوتاه (مثل یک شرکت) می‌خوردند.
همان درسی که در `scripts/groq_utils.py` گرفته بودیم، در سمت وب اعمال نشده بود.

رفع شد در سه لایه:
- `web/app/api/ask/route.js`: بودجه‌بندی متن (سقف ۱۰٬۰۰۰ کاراکتر، هر قطعه حداکثر ۱۶۰۰
  کاراکتر) و `maxTokens: 1500`. فهرست منابع هم فقط قطعه‌هایی را نشان می‌دهد که واقعاً به مدل
  داده شده‌اند، وگرنه شماره‌ی ارجاع‌های داخل جواب با فهرست منابع نمی‌خواند.
- `web/lib/groqFetch.js`: همان محافظ TPM سمت پایتون (تخمین توکن، کاهش خودکار سقف خروجی،
  خطای فارسی روشن اگر پرامپت واقعاً بزرگ باشد).
- پیام فارسی برای خطای ۴۱۳ اضافه شد (قبلاً متن خام انگلیسی Groq به کاربر نشان داده می‌شد).

**۵) موتور پشتیبان برای «پاسخ هوشمند».** حین تست، سهمیه‌ی روزانه‌ی رایگان Groq تمام شد و
دیدیم در آن حالت این قابلیت کلاً از کار می‌افتد — ریسک واقعی برای ارائه. حالا اگر Groq خطای
۴۲۹ بدهد، جواب با کلید جداگانه‌ی `GEMINI_API_KEY_ASK` داده می‌شود (همان کلیدی که از ۲۱ اوت
عمداً از ایجنت‌های روزانه جدا شده، پس رصد روزانه را نمی‌خواباند). تست شد: با وجود اتمام سهمیه‌ی
Groq، سؤال «رقیب اصلی ما در ترکیه کیست؟» جواب کامل با ۷ منبع گرفت.

**نکته‌ی روش‌شناسی:** اولین تست‌های `/ask` با curl همه «محتوایی پیدا نشد» می‌دادند و شبیه
خرابی کامل جست‌وجو به‌نظر می‌رسید. علت، خراب‌شدن متن فارسی در ارسال از طریق Git Bash بود، نه
باگ پلتفرم. با ارسال درخواست از پایتون (UTF-8 صریح) مشخص شد جست‌وجو سالم است و مشکل واقعی
جای دیگری است (همان ۴۱۳). درس: برای تست ورودی فارسی، از curl در Git Bash استفاده نکنیم.

**فایل‌ها:** `web/app/competitors/page.jsx`، `web/app/competitors/[id]/page.jsx`،
`web/app/api/ask/route.js`، `web/lib/groqFetch.js`

**هشدار برای ارائه:** سایتی که روی پورت ۳۰۰۰ در حال اجراست، نسخه‌ی *قبل* از این تغییرات را
سرو می‌کند. باید بسته و `اجرای-سایت.bat` دوباره اجرا شود تا پرچم‌ها و رفع باگ `/ask` دیده شوند.

---

### ۲۰۲۶-۰۸-۲۳ (۵) — افزودن پنج فید RSS تخصصی حمل‌ونقل به ربات خبر

**ورودی کاربر:** پنج فید RSS پیشنهادی (گرفته‌شده از Gemini) برای اخبار ترانزیتی، با درخواست
بررسی اینکه کدام قابل استفاده است.

**نتیجه‌ی تست مستقیم (همه در ۲۰۲۶-۰۸-۲۳ آزمایش شدند): هر پنج فید سالم‌اند.**
همه HTTP 200، XML معتبر، بدون نیاز به لاگین یا اشتراک، پاسخ زیر ۱.۵ ثانیه، و همه خلاصه‌ی
واقعی دارند (نه فقط تیتر):

| فید | آیتم | تازه‌ترین | میانه‌ی خلاصه | ارزیابی |
|---|---|---|---|---|
| Container News | ۱۰ | همان روز ۰۵:۳۰ | ۴۷۱ کاراکتر | تازه‌ترین و نزدیک‌ترین به کار ما (کانتینر/ترمینال) |
| The Loadstar | ۱۰ | ۱ روز قبل | ۵۸۹ کاراکتر | بلندترین خلاصه‌ها؛ لجستیک + تعرفه‌های تجاری |
| Splash247 | ۱۰ | ۲ روز قبل | ۳۶۰ کاراکتر | دریایی/بنادر؛ کانال پاناما و جریان تانکرها |
| Hellenic Shipping News | ۲۰ | ۲ روز قبل | ۳۶۰ کاراکتر | شاخص‌محور (گزارش هفتگی Xeneta، BDI) |
| gCaptain | ۱۲ | همان روز ۰۳:۳۷ | ۲۱۳ کاراکتر | پرنویزترین، ولی برای ژئوپلیتیک مسیرها (هرمز، دریای سرخ) ارزشمند |

**چرا هر پنج تا اضافه شدند، نه فقط بهترین‌ها:** این فیدها منبع **رویداد**اند (بستن کانال،
تحریم، ازدحام بندر، لغو سفر) در حالی که منابع HTML قبلی منبع **عدد**اند (FBX، WCI، BDI).
مکمل‌اند، نه جایگزین. نویز gCaptain هم با غربال کلیدواژه‌ای مهار می‌شود.

**چه ساخته شد:**

1. `fetch_utils.py`: مسیر جدید RSS — `fetch_rss` (پارس RSS و Atom)، `fetch_rss_sources`
   (چند فید + غربال ربط)، `build_rss_block` (تیتر + تاریخ + خلاصه + **لینک مستقیم مقاله**).
   لینک مستقیم مهم است چون پرامپت موظف است منبع هر نکته را با لینک بدهد؛ قبلاً از HTML
   خام فقط تیتر درمی‌آمد.
2. غربال ربط با **مرز کلمه**، نه زیررشته. نسخه‌ی اول زیررشته‌ای بود و «port» داخل
   «opportunity» و «rate» داخل «corporate» می‌افتاد — خبر ساخت کشتی تفریحی از غربال رد شد.
   با `\b` و پوشش جمع ساده (s/es) اصلاح و تست شد.
3. اگر غربال برای یک فید کمتر از سه آیتم بگذارد، سه خبر تازه‌ی همان فید بدون فیلتر فرستاده
   می‌شود — این یک غربال اولیه است، نه سانسور منبع؛ قضاوت نهایی با مدل.
4. `news_analysis_bot.py`: `RSS_NEWS_SOURCES` و `RSS_RELEVANCE_KEYWORDS` اضافه شد و بلوک
   فیدها به پرامپت وصل شد. قاعده‌ی جدید در پرامپت: لینک مستقیم مقاله را بده (نه آدرس فید)،
   و خلاصه‌ی فید متن شخص ثالث است — بازنویسی کن، کپی نکن.

**نتیجه‌ی اجرای واقعی:** دو خبر تازه با کیفیت محسوس بالاتر ثبت شد — «محدودیت عبور از کانال
پاناما و افزایش نرخ کرایه» (منابع: Splash247 + The Loadstar، با لینک مستقیم مقاله) و «تغییر
مسیرهای ترانزیت نفت در دریای سرخ و تنگه هرمز» (دو مقاله‌ی gCaptain). تحلیل اولی خودش به
تضعیف رقابت‌پذیری **چین** اشاره کرد — یعنی زمینه‌ی جدید رقبا هم دارد کار می‌کند.

**فایل‌ها:** `scripts/fetch_utils.py`، `scripts/news_analysis_bot.py`، `data/news_analysis_log.json`

---

### ۲۰۲۶-۰۸-۲۳ (۴) — افزودن کامل بازار ارمنستان (چهار منبع جدید)

**ورودی کاربر:** چهار فایل از پوشه‌ی `E:\مهرداد\سپهران\تحلیل بازار ارمنستان` — گزارش خلاصه‌ی
هوشمند، گزارش تحلیلی کامل، لیست شرکت‌ها، و لیست نمایشگاه‌ها.

**چه شد:** هر چهار فایل به پوشه‌ی `گزارش/` مخزن کپی و در سه پایپ‌لاین ingest ثبت شدند:

| منبع | خروجی |
|---|---|
| `گزارش_خلاصه_بازار_جوش_شیرین_ارمنستان.docx` | گزارش **هوشمند** (summary): ۵ بخش، ۴ آمار کلیدی، ۲ نمودار |
| `گزارش_تحلیلی_بازار_جوش_شیرین_ارمنستان.docx` | گزارش **تحلیلی کامل** (detailed): ۷۲ بلوک، عیناً بدون دخالت AI |
| `Armenia_Companies_1.xlsx` | ۱۳ شرکت (مجموع بانک: ۱۹۲) |
| `Armenia_Exhibitions_SepehranChemical.xlsx` | ۴ نمایشگاه (مجموع بانک: ۲۸) |

به‌علاوه پروفایل کشور با `enrich_countries.py` ساخته شد: کل واردات ۱٬۱۸۰ تن، و سه شریک
تجاری اصلی (ایران ۵۳٫۳٪ / روسیه ۲۸٫۷٪ / ترکیه ۷٫۱٪).

**سه چیزی که برای این کشور لازم شد و قبلاً وجود نداشت:**

1. **سرستون در سطر سوم:** فایل نمایشگاه‌های ارمنستان یک عنوان فارسی و یک سطر خالی بالای
   جدول دارد، ولی `load_xlsx_table` سرستون را همیشه از سطر اول می‌خواند. گزینه‌ی
   `header_row` (اندیس صفرمبنا) اضافه شد؛ پیش‌فرضش صفر است پس منابع قبلی دست‌نخورده‌اند.
2. **ستون «خریداران و غرفه‌داران بالقوه»:** این ستون در فایل‌های قبلی نبود و هیچ‌جای سایت
   رندر نمی‌شد. فیلد `audience` به اسکیما اضافه و در صفحه‌ی جزئیات نمایشگاه نمایش داده شد
   (فقط وقتی وجود دارد، پس نمایشگاه‌های قدیمی تغییری نمی‌کنند).
3. **اولین کشور محصور در خشکی:** ارمنستان بندر ندارد و `WorldRouteMap` با `port: null`
   کل نقشه را حذف می‌کرد. `LAND_ENTRY_POINTS` در `geo_data.py` اضافه شد (ایروان، ورود زمینی
   از مرز نوردوز/اگارک) و پروفایل حالا `kind: "land"` می‌گیرد. **عمداً وارد `PORTS` نشد**، چون
   آن‌وقت «فاصله‌ی هوایی تا بنادر مرجع» هم برایش حساب می‌شد و عدد گمراه‌کننده می‌داد — مسیر
   واقعی این بازار جاده‌ای است (شیراز → تبریز → نوردوز → ایروان، حدود ۱٬۹۰۰ کیلومتر طبق خود
   گزارش). نقشه نمایش داده می‌شود، ستون فاصله‌ها حذف و دلیلش زیر نقشه نوشته می‌شود.

**بررسی صحت داده:** عدد ۲٬۳۸۵ دلار بر تن برای ترکیه در نمودار مشکوک بود؛ متن سند اصلی چک شد و
واقعاً همان‌جاست (خود گزارش هم توضیح داده «به احتمال زیاد بسته‌بندی خرده‌فروشی، نه فله صنعتی»)
— یعنی ساختگی نیست. آمار گزارش با داده‌ی مستقل ITC Trade Map هم خوانده شد: هر دو ۱٬۱۸۰ تن و
۴۳۵ هزار دلار می‌دهند.

**فایل‌ها:** `گزارش/` (۴ فایل جدید)، `scripts/ingest_reports.py`، `scripts/ingest_companies.py`،
`scripts/ingest_exhibitions.py`، `scripts/geo_data.py`، `scripts/enrich_countries.py`،
`web/app/components/WorldRouteMap.jsx`، `web/app/exhibitions/[id]/page.jsx`،
`data/companies.json`، `data/exhibitions.json`، `data/country_profiles.json`،
`reports/manifest.json`، `reports/parsed/` و `reports/ارمنستان/`

**استفاده‌نشده:** در همان پوشه یک فایل
`Trade_Map_-_List_of_supplying_markets_for_the_product_imported_by_Armenia_in_2025.xls` هم
هست (تفکیک واردات ارمنستان به‌تفکیک مبدأ). داده‌ی کلی ارمنستان از قبل در `trade_map_2025.json`
هست، ولی این فایل جزئیات مبدأبه‌مبدأ دارد — اگر خواستید، یک پاس جدا می‌خواهد.

---

### ۲۰۲۶-۰۸-۲۳ (۳) — اصلاح اصطلاح «کانتینر ۴۰ فوتی» و به‌روزرسانی فهرست رقبا

**دو ایراد گزارش‌شده:**

1. در متن تحلیل اخبار نوشته شده بود «کانتینر ۴۰ **قدمی**». مدل «40-foot» را تحت‌اللفظی
   ترجمه کرده بود، در حالی که foot اینجا واحد طول (فوت) است و اصطلاح درست صنعتی
   «کانتینر ۴۰ فوتی» است.
2. متن خبرها فقط «رقابت با ترکیه» را می‌گفت، در حالی که رقبای تحت رصد الان **ترکیه و چین**
   هستند و **روسیه** هم به‌زودی اضافه می‌شود.

**چه کار شد:**

1. `news_analysis_bot.py`: بخش «اصطلاحات فنی» به پرامپت اضافه شد (۴۰/۲۰/۴۵ فوتی، TEU/FEU،
   فله‌ی خشک، نرخ کرایه‌ی حمل). چون تذکر در پرامپت تضمینی نیست، یک **تور ایمنی** بعد از
   تولید متن هم اضافه شد: `fix_terminology()` که روی تیتر، متن و key_facts اجرا می‌شود.
   الگو عمداً فقط «عدد + قدمی» را می‌گیرد تا عبارت‌های سالم فارسی مثل «قدمی مثبت» یا
   «در چند قدمی بندر» خراب نشوند (هر دو حالت تست شد).
2. دو رکورد موجود در `data/news_analysis_log.json` هم با همین تابع اصلاح شدند — الان صفر
   مورد «قدمی» در داده باقی مانده.
3. `COMPANY_CONTEXT` ربات خبر بازنویسی شد: ترکیه (رقیب نزدیک لجستیکی) و چین (بزرگ‌ترین
   تولیدکننده‌ی جهان) به‌عنوان رقبای تحت رصد، با تصریح اینکه روسیه به‌زودی اضافه می‌شود، و
   این قاعده که «هر جا از رقبا حرف می‌زنی فقط ترکیه را نام نبر — رقیبِ مرتبط با همان خبر
   را نام ببر».
4. همین ناهماهنگی در سه جای دیگر سایت هم بود و اصلاح شد:
   - صفحه‌ی اصلی: «رقیب اصلی: ترکیه» ← «رقبای تحت رصد: ترکیه و چین»
   - صفحه‌ی رقبا: متن معرفی هنوز می‌گفت فقط ترکیه ایجنت اختصاصی دارد، در حالی که چین از
     کامیت `f71e5ef` ایجنت خودش را دارد
   - `api/ask/route.js`: هم فهرست رقبا و هم یادداشت اصطلاح ۴۰ فوتی اضافه شد

**نکته‌ی مهم درباره‌ی خبرهای امروز:** متن سه خبر ثبت‌شده‌ی ۲۳ اوت از نظر اصطلاح اصلاح شد،
ولی چارچوب تحلیلی‌شان همچنان فقط ترکیه را نام می‌برد؛ بازتولیدشان نیاز به پاک کردن و اجرای
دوباره‌ی ربات داشت که انجام **نشد**. فهرست جدید رقبا از اجرای بعدی (فردا ۰۹:۰۰ تهران) در
متن خبرها ظاهر می‌شود.

**فایل‌ها:** `scripts/news_analysis_bot.py`، `data/news_analysis_log.json`، `web/app/page.js`،
`web/app/competitors/page.jsx`، `web/app/api/ask/route.js`

**برای وقتی روسیه اضافه شد:** سه جا باید هم‌زمان به‌روز شود — `COMPANY_CONTEXT` ربات خبر،
`MAIN_COMPETITORS` در `web/app/page.js`، و متن معرفی `web/app/competitors/page.jsx` (به‌علاوه‌ی
یک `russia_watch_bot.py` هم‌شکل با ترکیه/چین و ثبتش در workflow و `web/lib/data.js`).

---

### ۲۰۲۶-۰۸-۲۳ (۲) — بازرسی صفحه‌ی ترانزیت: ربات جمع‌آوری خاموش بود

**سؤال:** آیا هر دو بخش صفحه‌ی `/transit` (برآوردگر کرایه‌ی مبدأ-مقصد در بالا، اخبار تحلیلی
ترانزیت در پایین) واقعاً کار می‌کنند؟ و هر بخش از کدام سرویس هوش مصنوعی استفاده می‌کند؟

**یافته‌ی اصلی — ربات ترانزیت از ۲۲ اوت هیچ داده‌ای جمع نکرده بود.** علت: در فراخوانی Groq
`max_tokens=8000` گذاشته شده بود، در حالی که سقف TPM سرویس رایگان دقیقاً ۸۰۰۰ است و Groq
«توکن پرامپت + max_tokens» را **قبل از اجرا** با سقف می‌سنجد. یعنی درخواست همیشه ۸۰۷۲ توکن
حساب می‌شد و همیشه خطای ۴۱۳ می‌گرفت — داده‌ی ۲۱ اوت مربوط به اجرایی قبل از تنظیم این عدد بود.
ربات هم خطا را فقط `[WARN]` چاپ می‌کرد و یک رکورد **خالی** ثبت می‌کرد، که از بیرون شبیه
«امروز باری اعلام نشده» دیده می‌شد نه «ربات کار نکرد».

**مشکل دوم (بعد از رفع اول):** کانال amintara50 باز هم ناقص برمی‌گشت. مدل `gpt-oss-120b` قبل
از پاسخ، «توکن استدلال» تولید می‌کند که از همان max_tokens کم می‌شود و کل بودجه صرف استدلال
می‌شد. با `reasoning_effort: "low"` حل شد.

**مشکل سوم:** ۹ پست قیمت‌دار امروز داشتیم ولی فقط ۱ تای‌شان تناژ داشت، و بیشتر شهرها
(همدان، قزوین، گرگان، هشترود…) در فایل مختصات نبودند — پس عملاً هیچ نمونه‌ی تازه‌ای به
برآوردگر اضافه نمی‌شد.

**چه کار شد:**

1. `groq_utils.py`: سقف خروجی قبل از ارسال با فضای باقی‌مانده‌ی TPM هماهنگ می‌شود؛ روی ۴۱۳
   خودکار نصف می‌شود و روی ۴۲۹ صبر و تلاش مجدد می‌کند؛ پارامتر `reasoning_effort` اضافه شد.
2. `transit_watch_bot.py`: `max_tokens=5000` + `reasoning_effort="low"`؛ نجات آیتم‌های کامل از
   پاسخ ناقص به‌جای دورریختن کل کانال؛ و دیگر رکورد خالی ثبت نمی‌کند (به‌جایش `[ERROR]` می‌دهد).
3. `data/transit_places.json`: از ۲۵ به ۱۱۹ مکان رسید — مراکز استان‌ها، شهرهای پرتردد باری،
   مرزهای زمینی (شلمچه، مهران، پرویزخان، بیله‌سوار، جلفا، اینچه‌برون، دوغارون…) و مقاصد منطقه‌ای
   (ترکیه، عراق، قفقاز، آسیای میانه، روسیه، افغانستان). فاصله‌ی محاسبه‌شده از ۲ به ۳۱ پست رسید.
4. نرخ جدید `rate_per_km` (تومان بر کیلومتر برای یک کامیون) اضافه شد، چون بیشتر پست‌های
   اعلام‌بار کرایه‌ی کل ماشین را می‌گویند و تناژ نمی‌نویسند. هیچ حدسی درباره‌ی وزن بار زده نمی‌شود.
5. `web/lib/data.js`: حذف پست‌های تکراری بین روزها (یک آگهی چند روز روی کانال می‌ماند و هر روز
   دوباره برداشت می‌شد و نمونه را ساختگی بزرگ می‌کرد) — اثرانگشت = مسیر + تناژ + مبلغ، **بدون**
   note چون مدل هر روز آن را با جمله‌بندی متفاوت خلاصه می‌کند. محاسبه از میانگین به **میانه**
   تغییر کرد (نمونه کوچک است و یک مسیر ترانزیتی گران کل میانگین را جابه‌جا می‌کرد).
6. `TransitEstimator.jsx`: کرایه‌ی یک کامیون به‌عنوان عدد اصلی، بازه‌ی کمینه-بیشینه‌ی مشاهده‌شده،
   و رقم به‌ازای هر تن فقط وقتی نمونه‌ی تناژدار وجود دارد (با ذکر تعداد نمونه).
7. `.github/workflows/daily_market_agents.yml`: `continue-on-error: true` روی هر پنج ربات —
   قبلاً خطای یک ربات کل job را fail می‌کرد و مرحله‌ی commit اجرا نمی‌شد، یعنی داده‌ی سالم
   بقیه هم همان روز از دست می‌رفت.

**وضعیت فعلی صفحه:** هر دو بخش کار می‌کنند. اجرای امروز ۲۳ پست از دو کانال (۹ با قیمت صریح)
آورد؛ بعد از حذف تکراری‌ها ۳۷ پست یکتا و ۱۰ نمونه‌ی نرخ معتبر داریم (میانه ≈ ۲۳٬۴۵۸ تومان بر
کیلومتر برای یک کامیون؛ بازه‌ی ۹٬۲۰۰ تا ۱۰۴٬۷۶۲ — سر بالای بازه مسیر ترانزیتی آستارا-خسروی با
CMR است). نمونه‌ی تناژدار هنوز فقط ۱ عدد است و در UI هم همین‌طور اعلام می‌شود.

**کدام سرویس هوش مصنوعی:** خودِ محاسبه‌ی برآوردگر **هیچ AI ندارد** (هاورساین + میانه، در مرورگر).
جمع‌آوری و ساختاردهی پست‌های کانال‌های اعلام‌بار با **GROQ_API_KEY** (مدل `openai/gpt-oss-120b`)
انجام می‌شود، و اخبار تحلیلی پایین صفحه از ربات خبر می‌آید که با **GEMINI_API_KEY**
(مدل `gemini-3.6-flash`) کار می‌کند.

**فایل‌ها:** `scripts/groq_utils.py`، `scripts/transit_watch_bot.py`، `data/transit_places.json`،
`data/transit_log.json`، `web/lib/data.js`، `web/app/transit/page.jsx`،
`web/app/transit/TransitEstimator.jsx`، `.github/workflows/daily_market_agents.yml`

---

### ۲۰۲۶-۰۸-۲۳ — رفع یخ‌زدگی اخبار سایت (ربات خبر چندموضوعی شد)

**مشکل گزارش‌شده:** چند روز بود اخبار سایت آپدیت نمی‌شد و همان خبر «کاهش قیمت سودا اش» ثابت
مانده بود؛ خبر تازه‌ای از بازار جهانی، صنعت جوش شیرین و حوزه‌ی ترانزیت نمی‌آمد.

**تشخیص (با شواهد، نه حدس):**

- آخرین رکورد `data/news_analysis_log.json` مربوط به ۱۹ اوت بود، در حالی که `price_history.json`،
  `turkey_watch_log.json`، `china_watch_log.json` و `transit_log.json` تا ۲۲ اوت به‌روز بودند —
  یعنی workflow اجرا می‌شد و فقط **ربات خبر** خروجی نمی‌داد.
- هر ۸ منبع خبری دستی تست شد: **همه سالم و در دسترس بودند** (dailysabah، tradingeconomics
  soda-ash و baltic، chemxplore، chemeurope، oilprice، freightos، drewry). پس مشکل از منابع نبود.
- ربات با کلید واقعی و بدون نوشتن در فایل اجرا شد؛ خروجی خام مدل دقیقاً این بود:
  `{"has_new_content": false, ...}` — یعنی علت پیدا شد.

**علت ریشه‌ای:** منطق ضدتکرار (کامیت `577658d`) کل تصمیم را به مدل سپرده بود و روزی فقط **یک**
تحلیل تولید می‌شد. شاخص سودا اش TradingEconomics چند روز روی ۱۰۳۰ یوآن ثابت مانده بود، پس مدل
هر روز «تکراری» اعلام می‌کرد و **کل روز** حذف می‌شد — از جمله خبرهای واقعاً تازه‌ی حمل‌ونقل، انرژی
و صنعت. صفحه‌ی `/transit` هم چون اخبارش را از همین فایل فیلتر می‌کند، هم‌زمان یخ زده بود.

**چه کار شد:**

1. `scripts/news_analysis_bot.py` بازنویسی شد: به‌جای یک تحلیل در روز، تا ۴ **آیتم خبری مجزا** در
   چهار موضوع (بازار سودا اش / صنعت و رقبا / ترانزیت و کرایه‌ی حمل / انرژی و ژئوپلیتیک). ثابت‌ماندن
   یک موضوع دیگر بقیه را خفه نمی‌کند.
2. تشخیص تکرار از دست مدل گرفته و به خود پایتون داده شد: اثرانگشت عددی (`key_facts` + اعداد متن،
   با نرمال‌سازی ارقام فارسی) به‌علاوه‌ی شباهت Jaccard تیتر، در پنجره‌ی ۱۲ روزه و **به‌ازای هر آیتم**.
   تست شد: خبر تکراری سودا اش → رد، خبر ترانزیت تازه → قبول، سودا اش با عدد جدید → قبول.
3. **باگ جدی‌تری هم پیدا شد:** `fetch_sources` فقط `URLError/HTTPError/TimeoutError` را می‌گرفت.
   منبعی که وسط پاسخ اتصال را ببندد `RemoteDisconnected` می‌دهد که زیرمجموعه‌ی این‌ها نیست و
   **کل ربات را crash می‌کرد** (احتمال زیاد دلیل نبود اجرای ۲۱ اوت). حالا `OSError` و
   `http.client.HTTPException` گرفته می‌شود و یک بار تلاش مجدد با مهلت بلندتر انجام می‌شود
   (chemeurope و drewry امروز در تلاش اول time out شدند و در تلاش دوم آمدند).
4. سایت: برچسب موضوع روی کارت خبر، دکمه‌های فیلتر موضوعی در صفحه‌ی اخبار، و فیلتر صفحه‌ی ترانزیت
   حالا اول بر اساس `topic` کار می‌کند و کلیدواژه‌ها به‌عنوان مکمل باقی ماندند (تا خبر ژئوپلیتیکِ
   مرتبط با تنگه هرمز هم از دست نرود). رکوردهای قدیمی که `topic` ندارند سالم رندر می‌شوند.

**نتیجه‌ی اجرا:** ۳ خبر تازه برای ۲۳ اوت ثبت شد — تثبیت سودا اش چین، جهش ۱.۸ درصدی شاخص بالتیک
(BDI ۲۸۴۱ و FBX ۳۵۷۰.۸ دلار)، و رشد نفت برنت به ۹۴.۳۹ دلار با اشاره به کندی تردد در تنگه هرمز.
`npm run build` سایت بدون خطا کامل شد.

**فایل‌ها:** `scripts/news_analysis_bot.py` (بازنویسی)، `scripts/fetch_utils.py`،
`web/app/components/NewsCard.jsx`، `web/app/news/NewsClient.jsx`، `web/app/transit/page.jsx`،
`data/news_analysis_log.json`

**نکته برای آینده:** آستانه‌های تشخیص تکرار (`DEDUP_LOOKBACK_DAYS`، آستانه‌ی ۰.۳۰ شباهت تیتر) عمداً
محافظه‌کارانه‌اند تا دوباره چیزی یخ نزند؛ اگر خبرهای خیلی شبیه به هم دیدید، همان‌ها را سخت‌گیرتر کنید.

---

### ۱۴۰۵/۰۵/۳۱ — ۲۰۲۶-۰۸-۲۲ — راه‌اندازی همین کارنامه

**چه شد:** فایل `WORKLOG.md` ساخته شد تا از این به بعد هر مرحله‌ی کاری که پوش می‌شود اینجا مستند
شود. هم‌زمان وضعیت مخزن بررسی شد: درخت کاری تمیز، لوکال و ریموت هم‌تراز روی کامیت `8e8d6fa`،
۴۹ کامیت تا این لحظه.

**چرا:** چت قبلی حین بررسی یک طرح در Canva کرش کرد و کامل پاک شد. برای اینکه با باز کردن پنجره‌ی
جدید زمینه‌ی کار از دست نرود، تاریخچه باید بیرون از چت — روی دیسک و گیت‌هاب — ثبت شود.

**فایل‌ها:** `WORKLOG.md` (جدید)

---

## خلاصه‌ی تاریخچه‌ی پیش از این کارنامه (کامیت‌های ۱ تا ۴۹)

بازسازی‌شده از تاریخچه‌ی گیت، برای اینکه کارنامه از صفر شروع نشود:

1. **راه‌اندازی اولیه** (`cafe257`) — ایجنت‌های قیمت/اخبار مبتنی بر web_search، سایت Next.js
   (داشبورد + مخزن گزارش‌ها) و workflow روزانه‌ی گیت‌هاب.
2. **مهاجرت موتور هوش مصنوعی** (`67f5c42`, `bff6d6d`) — از Claude پولی به Gemini؛ سپس کنار گذاشتن
   «Grounding with Google Search» به‌خاطر خطای ۴۲۹ روی کلیدهای رایگان و رفتن به معماری
   «fetch + تحلیل» (`fetch_utils.py`).
3. **بانک محتوا** (`6694d00`, `730ba43`, `eea0086`) — بانک شرکت‌ها و نمایشگاه‌ها، پایپ‌لاین دو نوع
   گزارش (مفصل/مدیریتی هوشمند)، تفکیک گزارش‌های هوشمند از آرشیو، پروفایل کشور و نقشه‌ی مسیر.
4. **طراحی و برند** (`30bcbad`, `12f2644`) — برندینگ سپهران شیمی، سیستم رنگ/تایپوگرافی، نقشه‌ی
   گوگل‌مپ واقعی، بازطراحی داشبورد قیمت و اخبار طبق فیدبک.
5. **تحلیل رقبا** (`5392bf3`, `f71e5ef`, `788ba92`) — بخش رقبا با صفحه‌ی اختصاصی ترکیه، سپس چین و
   افزودن «شیمیایی بویان»، و تعمیم رصد زنده به هر رقیب.
6. **دیپلوی و اجرای محلی** (`99acdc3`, `7c94e17`, `d90a655`, `096beca`, `33df10e`) — تلاش Vercel،
   اجرای شبکه‌ی داخلی، Docker/Liara، فایل‌های `.bat` و رفع باگ مسیر فارسی با short path.
7. **حمل‌ونقل و ترانزیت** (`c0c0f59`, `4ef4096`, `0fa9924`, `044b1b3`, `8e8d6fa`) — منابع کرایه‌ی
   کانتینری (Freightos FBX، Drewry WCI)، Baltic Dry Index، بخش «تحلیل ترانزیت»، و در آخرین کامیت
   بازطراحی آن: حذف جدول خام تلگرام و افزودن برآوردگر هوشمند کرایه‌ی مبدأ/مقصد + اخبار فیلترشده.
8. **جداسازی سهمیه‌ها و Groq** (`f47c96b`, `2b79da0`, `0156b7d`, `4c91d80`) — تفکیک توکن‌های Gemini
   و انتقال کامل `/ask` به Groq.
9. **داده‌ی واقعی بازار** (`3bebc79`, `1217f2c`, `fd4bc8c`) — بازطراحی بخش کشورها با ITC Trade Map
   2025، اولین نقطه‌ی داده‌ی Intratec، صفحه‌ی کامل بازار سوریه.
