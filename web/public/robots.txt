# این یک پلتفرم داخلی شرکت است، نه سایت عمومی.
# لایه‌ی اصلی محافظت، رمز عبور (middleware.js) است؛ این فایل فقط یک لایه‌ی اضافی
# برای خزنده‌هایی است که ممکن است به هر دلیلی به صفحه‌ای دسترسی پیدا کنند.
User-agent: *
Disallow: /
