"""
مختصات بنادر اصلی + محاسبه‌ی فاصله‌ی هوایی (great-circle) بین‌شون.

نکته‌ی صداقت: این فاصله‌ها خط‌مستقیم (هوایی) هستن، نه مسیر واقعی کشتی‌رانی
(که به خاطر خطوط ساحلی/کانال‌ها معمولاً بلندتره). برای یک تخمین نسبی و مقایسه‌ای
بین رقبا کافیه، ولی نباید به‌عنوان مسافت دقیق دریایی ارائه بشه.

مختصات بنادر مرجع (چین/ترکیه/هند/روسیه/ایران) از منابع عمومی (ویکی‌پدیا و
دیتابیس‌های بندری) گرفته شده و تأیید شده‌ن. بنادر مقصد (برزیل/کنیا/عراق/اردن)
از همون بندری هستن که توی متن گزارش‌های واقعی شرکت ذکر شده.
"""

import math

# {کد کشور به فارسی: (lat, lon, نام بندر)}
PORTS = {
    "ایران": (27.1142, 56.0614, "بندر شهید رجایی (بندرعباس)"),
    "چین": (31.2304, 121.4737, "شانگهای"),
    "ترکیه": (36.8000, 34.6333, "مرسین"),
    "هند": (18.9490, 72.9525, "بندر جواهر لعل نهرو (JNPT، بمبئی)"),
    "روسیه": (44.7239, 37.7683, "نووروسیسک"),
    "برزیل": (-23.9608, -46.3339, "سانتوس"),
    "کنیا": (-4.0435, 39.6682, "مومباسا"),
    "عراق": (30.0534, 47.9376, "ام‌القصر"),
    "اردن": (29.5167, 35.0000, "عقبه"),
    "سوریه": (35.5211, 35.7822, "لاذقیه (Latakia)"),
}

# کشورهای محصور در خشکی «بندر ورودی» ندارن. برای این‌ها نقطه‌ی مرجع، شهر مقصد
# اصلی (گمرک ورودی) است — فقط برای نمایش روی نقشه.
#
# عمداً وارد PORTS نشدن: اگر آن‌جا بودند، «فاصله‌ی هوایی تا بنادر مرجع» هم برایشان
# محاسبه می‌شد و عدد گمراه‌کننده‌ای می‌داد. مسیر واقعی این بازارها جاده‌ای است
# (مثلاً ارمنستان: شیراز → تبریز → مرز نوردوز/اگارک → ایروان، حدود ۱٬۹۰۰ کیلومتر
# جاده‌ای طبق گزارش تحلیلی)، نه خط‌مستقیم از یک بندر.
LAND_ENTRY_POINTS = {
    "ارمنستان": (40.1792, 44.4991, "ایروان (ورود زمینی از مرز نوردوز/اگارک)"),
}


def haversine_km(lat1, lon1, lat2, lon2):
    R = 6371.0
    p1, p2 = math.radians(lat1), math.radians(lat2)
    dphi = math.radians(lat2 - lat1)
    dlambda = math.radians(lon2 - lon1)
    a = math.sin(dphi / 2) ** 2 + math.cos(p1) * math.cos(p2) * math.sin(dlambda / 2) ** 2
    return round(R * 2 * math.atan2(math.sqrt(a), math.sqrt(1 - a)))


def distance_between(country_a, country_b):
    if country_a not in PORTS or country_b not in PORTS:
        return None
    lat1, lon1, _ = PORTS[country_a]
    lat2, lon2, _ = PORTS[country_b]
    return haversine_km(lat1, lon1, lat2, lon2)
